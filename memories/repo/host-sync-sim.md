# host-sync-sim

- Shim set that proved sufficient: `pico/stdlib.h` (+ inline `absolute_time_diff_us`), `pico/types.h`, `hardware/pio.h` (opaque), `hardware/adc.h`, `hardware/flash.h`, `hardware/sync.h`, `pico/flash.h`, `hardware/gpio.h`, `hardware/pwm.h` (must `#include "hardware/gpio.h"` — real SDK pulls it in transitively and `cutter.c` relies on that), `hardware/clocks.h`. No firmware edits.
- Design doc's own "104 symbols, 4 sync files only" link surface was measured before the scope decision to compile `motion.c`/`toolchange.c`/`cutter.c` too — stale by the time of implementation. Real undefined-symbol surface (motor/lane/servo/tmc2209-apply functions) is smaller than expected: `motor_set_rate_sps`/`motor_enable`/`motor_set_dir`/`motor_stop`/`lane_start`/`lane_stop`/`motion_clamp_rate_sps`/`cutter_busy`/`tc_state`/`reload_trigger` are REAL (defined in the linked TUs), not faked. Only `lane_ptr`/`set_active_lane`/`set_toolhead_filament`/`other_lane` (main.c-owned) and `cmd_event`/`cmd_event_critical`/`manual_unload_active` (protocol.c-owned) plus 4 tmc2209.c entry points and a handful of main.c pure helpers (`clamp_i`/`clamp_f`/`lane_to_idx`/`sps_to_mm_per_min`/`mm_per_min_to_sps`/`build_ihold_irun_reg`) needed fakes. Lesson confirmed: regenerate the undefined-symbol list from the real linker, never trust a design doc's earlier measurement once scope changes.
- `lane_t.current_sps` + `lane_t.task_forward` are real firmware state the plant reads directly for commanded feed — no need to decode fake PWM registers for motor rate/direction. Simplified the plant a lot versus the original design sketch.
- Two normative spec.md scenarios were WRONG and got corrected during implementation, not just deviated from: (1) "retract saturates the tension rail" — the plant's own `slack_mm += (feed - demand) * dt` formula (verified correct against the other two Buffer Plant Model scenarios) makes negative demand drive slack toward *compression*, not tension; fixed the spec text. (2) invariant 2 "bounds" required `[min_sps, max_sps]` — `sync.c:1898`'s actual final clamp is `[0, max_sps]`; `g_sync_min_sps` is only an intermediate floor on specific paths, so sub-min transients during ramp-up/smoothing are legitimate. Both caught by actually running the sim against real sync.c, not by inspection.
- Invariant 4 (fault quiescence) needed an exemption for the tick where `SYNC_FAULT_HOLD` is first entered — the firmware's own entry-announcement `cmd_event` fires that tick and is expected; it's continued emission on *later* ticks that's the deadlock signature.
- `-Wall -Wextra` scoped to `tests/host/`'s own sources only via `set_source_files_properties`, not the whole `flare_sim` target — firmware/src/*.c has pre-existing unused-parameter/sign-compare warnings under `-Wextra` that are out of scope (zero firmware edits) and not mine to fix.
- Gap (closed for H4/H5/H6, still open otherwise): initially the scenario catalogue didn't reproduce the historical RELOAD/BL-follow regressions by name. Follow-up session added `reload_genuine_runout_escalation` (H6), `reload_idle_consumer_staged_completion` (H4), `reload_already_loaded_noop` (H5) driving the real `toolchange.c` RELOAD state machine end to end — all three pass, giving `audit-reliability-fixes`' H4/H5/H6 real sim-level evidence its remaining HW:-gated tasks (10.3/11.3/12.4) can't self-certify. Type-P RELOAD sign regression and stale-fault-timers-while-OFF are still unmodeled.
- `scripts/validate_regression.py`'s ruff step currently fails on pre-existing `flare_daemon.py` findings (F841, unrelated files, confirmed via `git diff` against main) — not introduced by this change, but it means the full gate script doesn't exit 0 end-to-end right now. The new "Host Sync Simulation" gate step itself (build + `unittest scripts.test_sync_sim`) passes cleanly in isolation.
- Building the RELOAD scenarios surfaced three more real bugs, all found by running the sim against real `sync.c`/`toolchange.c`, not by inspection:
  1. `lane_id` was never initialized (0 for both lanes) because `lane_setup()` — which sets it in real firmware boot — is never called (motor/GPIO init is meaningless over the fakes). Silently broke `lane_to_idx`/`reload_trigger`'s other-lane lookup on EVERY prior scenario too (harmless only because both lanes share identical default gearing). Fixed by seeding `g_lane_l1.lane_id=1`/`g_lane_l2.lane_id=2` directly in `sim_main.c`'s boot sequence.
  2. `feed_gain` fault injection was applied to the *summed* two-lane feed, not per-lane — a RELOAD scenario's lane-1 jam fault silently zeroed the swapped-in lane 2's legitimate approach feed too. Fixed: `feed_gain` now applies only to the scenario's originally-active lane's own contribution (sim_plant.c).
  3. `g_extruder_est_sps` is stale during the whole RELOAD sequence (the only thing that updates it, `sync_tick`, is suppressed while `tc_busy()`) — a scenario meant to isolate H6 tripped an unrelated `FOLLOW_JAM` because the pre-runout estimate read as "consumer active" to `tc_reload_consumer_active()`. Real interaction between H4 and H6, not a bug in either fix; the H6 scenario now sets `force_no_consumer` to isolate it cleanly. Worth deeper investigation on whether real firmware resets/decays this estimate on entering RELOAD — not chased down this session.
  4. The CSV `events` column was never CSV-quoted despite containing commas (`"TYPE,DATA"`), so plain `csv.DictReader` silently shifted every column after it on any row with an event — a latent bug affecting every prior python-side zone/sat assertion on rows with events, not just the new RELOAD tests (values happened to still pass by luck, not correctness). Fixed by quoting the field in `sim_trace.c`.
- Second follow-up session: derived scenarios directly from `openspec/specs/sync-state-model`'s `#### Scenario:` blocks (22 total; most are doc/taxonomy-only or protocol.c-gated and out of sim scope — `RA:`/BL:-command scenarios need protocol.c, not linked). Two runtime-testable ones added: `sem_relief_pause_lifecycle` ("Enter relief pause without losing state" + "Resume on TENSION re-arm" — pass, real `SYNC,AUTO_START` re-arm confirmed) and `sem_fault_hold_standalone_recovery` ("Standalone recovery" — pass, `SYNC,FAULT_HOLD_RECOVERY` fires exactly `CONF_SYNC_FAULT_HOLD_RECOVERY_MS` after entry, no host command).
- Initially misread `sem_fault_hold_standalone_recovery`'s trace as a deadlock (recovery starved by a rail-guard resetting the entry timer every tick) — wrong on closer inspection. What actually happens: recovery fires exactly on schedule; because the scenario's jam is *permanent* (`feed_gain` never restored), the plant can't physically move even with real commanded feed after recovery, so it re-saturates and re-enters `FAULT_HOLD` within ~1.5s, repeating the entry/recover/re-enter cycle forever — correct, designed behavior for a genuinely unrecoverable jam, not a defect. Lesson: a generic invariant (6, saturation-bound, 20s) firing doesn't automatically mean the mechanism under test is broken — check whether the scenario itself models a condition expected to never clear before calling something a bug. Verify twice before reporting a "found a defect" claim.
- BL:T/BL:C buffer-lock lifecycle scenarios ("Host requests buffer lock", "Catch sub-state engages on raw departure", "Locked sub-state ignores idle buffer changes", "Host clears buffer lock") are directly callable (`sync_buffer_lock_arm`/`sync_retract_assist_set` don't need protocol.c) but not yet built — same trigger-at-scripted-time mechanism as `manual_reload_at_ms` would work. Follow-up, not attempted this session.
- Third follow-up session: covering "all specs" turned out to mean ~15 of 42 specs are even runtime-reachable (~240 of 499 total `#### Scenario:` blocks) — the rest are Klipper/python-daemon/protocol-wire/process-docs specs `flare_sim` structurally can't exercise (no protocol.c, no Klipper, no python linked). Agreed to work through in-scope specs one at a time rather than attempt all at once.
- Built `openspec/specs/buffer-state-lock` (type-D BL:T/BL:C lifecycle) next: `sem_bl_lock_catch`, `sem_bl_release_via_bs`, `sem_bl_watchdog_timeout` — all pass. This required a real architecture fix first: BL prime/lock/catch (`sync_buffer_lock_arm` et al) drives `motor_set_rate_sps()`/`motor_set_dir()` **directly** against `lane->m`, never touching `lane->task`/`current_sps`/`task_forward` — the plant was completely blind to BL-driven motion (same failure class as the RELOAD `feed_gain` bug, worse: total blindness, not partial). Fixed by decoding commanded rate from the PWM fake's `clkdiv`/`wrap` (`sim_motor_rate_sps()` in sim_fakes.c, inverting motion.c's own `freq = sys_clk/(clkdiv*(wrap+1))`) plus `gpio_get(dir_pin)` for direction, used as a fallback when `lane->task == TASK_IDLE`. Needed real per-lane motor wiring too — `motor_init()` was never called (lane->m.slice/chan defaulted to 0 for both lanes, aliasing in the PWM fake array); now called directly in `sim_main.c` boot with the real `PIN_L1_*`/`PIN_L2_*` constants (harmless — motor_init's gpio_*/pwm_* calls are all no-op fakes).
- Found a THIRD spec/code mismatch this way: buffer-state-lock's "Extruder retract breaks the lock" scenario says `EV:BL:BREAK` is emitted; real firmware emits `EV:BL:FOLLOW` (grep `cmd_event.*"BL"` in sync.c — no `BREAK` event exists anywhere). Did NOT hand-edit `openspec/specs/buffer-state-lock/spec.md` — unlike host-sync-sim's own change-scoped spec.md fixed earlier, this is a live/merged spec outside any active change, so direct edits aren't the sanctioned path here. Flagged instead; python test asserts the real event name with a docstring note.
- `sem_bl_watchdog_timeout` (BL_WATCHDOG_DEFAULT_MS = 30000ms, sync.c) raced my own generic liveness invariant (SIM_LIVENESS_BACKSTOP_MS, also 30000ms) — the invariant measures from `SYNC_RETRACT_ASSIST` entry, the watchdog from `BL_LOCKED` entry (~160ms later in that run), so my invariant fired ~160ms before the real watchdog. Not a firmware bug — coincidental collision between an arbitrarily-chosen generic backstop and a real 30s timeout. Fixed by raising `SIM_LIVENESS_BACKSTOP_MS` to 45000ms (comfortable headroom above every known real sync-lifecycle timeout); no spec value was pinned to 30000 so this was a free implementation choice, not a spec change.
- Fourth session, picked `openspec/specs/cutter-feed-timeout` (small, bounded, `cutter.c` untested by any prior scenario). Added `sem_cutter_large_feed_completes`, `sem_cutter_feed_timeout_jam`, `sem_cutter_settle_completes`, `sem_cutter_settle_timeout_abort` — all pass, no new plant/architecture work needed (`cutter_start()`'s feed drive goes through the same BL-style motor-level fallback already built; `cutter_tick()`'s phase timeouts are pure elapsed-ms comparisons, no plant interaction). Added scenario-level tunable overrides (`cut_feed_mm_override`, `cut_timeout_feed_ms_override`, `servo_settle_ms_override`, `cut_timeout_settle_ms_override`, applied in `sim_main.c` right after `settings_defaults()`) since these scenarios need config combinations (e.g. timeout below feed duration) that aren't `main.c` defaults.
- Found a 4th spec/code mismatch: cutter-feed-timeout's two timeout scenarios both say `cutter_abort()` is called and `CUT:ERROR ABORTED` is emitted. Real code calls `cutter_fail(reason)` instead (`cutter_abort()` is a distinct external-abort-only entry point, never reached from a phase timeout), emitting `CUT:ERROR,<reason>` (`FEED_TIMEOUT`/`OPEN_TIMEOUT`/`CLOSE_TIMEOUT`/`REOPEN_TIMEOUT`) — not literally `ABORTED`. Same call as buffer-state-lock's `BL:BREAK`/`BL:FOLLOW` mismatch: not hand-edited (live spec, no active change), flagged in the python test docstring, asserted against the real event text.
- Running scenarios batch-by-batch without their curated `--ticks` value gave a false "regression" (both_switches_fault hit invariant 6 at the 60s default instead of the tested 8s) — not a real regression, just an inconsistent shell loop. Always double-check a suspected regression reproduces with the exact same invocation before reporting it as real.
- Fifth session (user authorized proceeding autonomously through specs, stop only on real implementation issues), picked `openspec/specs/motion-safety`. Added `sem_motion_dry_spin_probe` (`FAULT:DRY_SPIN` after 8s of TASK_FEED with both switches clear and buffer not in TENSION — real, confirmed working). Skipped "Task Travel Limits"/`LOAD_TIMEOUT` (`TASK_LOAD_FULL` has enough interacting preconditions — OUT-sensor latch, distance-bypass gates, sensor timeout races — that a clean isolated scenario needs more setup than the time budget allowed; not attempted, not a gap I'm hiding) and "Safe Autopreload" (confirmed untestable, `autopreload_tick()` is main.c-static).
- Found the SIGNIFICANT 5th finding this session, this one structural not just a spec-wording mismatch: `sync.c`'s "hard-wall critical" `FAULT_HOLD` escalation (`sync_tick_apply_rate`, ~line 1820) computes `compression_wall_critical` only under `g_buf_sensor_type == BUF_SENSOR_TYPE_D`, then only *acts* on it (`sync_fault_hold()`) when sensor type is NOT D (`compression_wall_critical && g_buf_sensor_type != BUF_SENSOR_TYPE_D`) — the AND can never be true, so this specific FAULT_HOLD path is permanently unreachable for both sensor types. Confirmed empirically, not just by reading: `idle_zero`/type-D drives a ~63 mm/s compression push (thresholds are 0.25 mm/s / 350 ms — both blown through) and `SYNC,FAULT_HOLD` never fires; the relay control law backs the feed off before the rail is hit instead. The code's own comment explains the *intent* (suppress this specific escalation for type-D because it was causing a FAULT_HOLD/recovery-slam cycle, relay handles compression differently now) but the way it's written makes it unreachable outright rather than "type-D exempted, type-P still guarded" as the comment and motion-safety's "Hard-wall critical triggers FAULT_HOLD" scenario both imply. Regression-guard test added (`test_hard_wall_critical_fault_hold_path_is_dead_code`) asserting current (not spec-stated) behavior — worth someone with firmware authority deciding whether this is intentional-and-the-spec-is-stale, or a real gap (type-P currently has NO hard-wall escalation of its own besides the analog-rail-saturation path already covered by `sem_fault_hold_standalone_recovery`, which is a different mechanism).
- Mid-session, user asked to switch to SDD (OpenSpec) tracking instead of ad-hoc inline work, "to avoid overfilling context". Created change `spec-derived-sim-coverage` (proposal/design/specs/tasks) to carry the remaining ~11-spec backlog as a resumable task list rather than requiring full conversation replay. Recorded the 4 already-done specs (sync-state-model, buffer-state-lock, cutter-feed-timeout, motion-safety) as task group 1 for traceability.
- `relay-fallback-only` (task 2, done): added `sem_relay_fallback_probe` (type-D, steady demand, exercises all 3 `relay_control_law()` branches). Found the 6th spec/code mismatch: "Catch-up and stop branches preserved" says COMPRESSION drives `SYNC_MIN` (682 default); real `relay_control_law()` (sync_relay.c) literally `return 0` for `BUF_COMPRESSION` — confirmed empirically via `idle_zero`/type-D, which converges to and holds exactly 0 for the full 56s+ sustained-compression tail of that run, never 682. Skipped: build-symbol/protocol-telemetry/analyzer-output/config-generator scenarios (build/protocol.c/python, none linked into `flare_sim`).
- `toolchange-orchestration` (task 10, partial): RELOAD scenarios (H4/H5/H6, already built) cover "RELOAD Buffer-Driven Contact"/"RELOAD Bang-Bang Pressure Cycle". Added `tc_start_at_ms`/`tc_target_lane` scenario trigger fields (real, working) to drive the plain non-RELOAD `tc_start()` path, but the built scenario stalled: unload phase needs `OUT` to go true->false as unload physically progresses, needs more `switch_script` timing than one t=0 event gives — same precondition-complexity class as `TASK_LOAD_FULL` (also skipped, motion-safety). Removed the broken scenario rather than ship a failing one. `sync-feedback`/`sync-refactor-foundation`/`reserve-safety-floor`/`buffer-geometry-vocabulary`/`flow-keyed-schedule` all disposition-only (no new scenario) — either pure code-structure/process/config-generator scope (not runtime-observable), or verified correct by direct code read with the repo's degenerate 1-point flow schedule limiting further runtime differentiation.
- `persistence-contract` (task 4, done): most requirements static/structural, already covered by `scripts/test_settings_parity.py`. Only "Fresh Board" is runtime-observable — added `sem_persistence_fresh_board` + `PersistenceContractTests`. Found the 8th mismatch: spec says fresh/invalid-magic settings get "written back to flash immediately"; real `settings_load()` calls `settings_defaults()` on that path and just returns — zero `settings_save()` calls in it. Confirmed by direct flash-byte comparison before/after in the sim (`written_back=0`). New mechanism: `sim_scenario_t.test_settings_load_fresh_board` makes `sim_main.c` call real `settings_load()` instead of `settings_defaults()` at boot, against the pristine zeroed RAM-flash fake (every process starts with zeroed flash, matching an erased board's invalid magic naturally).
- `type-d-dynamic-flow` (task 3, disposition-only): the MOST significant mismatch found this project. Requirement "A tension touch slams a decaying recovery feed floor" names `SYNC_TENSION_RECOVERY_FLOOR`/`SYNC_TENSION_RECOVERY_MS` and a fixed-MS-decay-after-snap mechanism — `grep -rn "SYNC_TENSION_RECOVERY" firmware/` returns nothing, those symbols don't exist anywhere. The real shipped mechanism is `sync_apply_type_d_probe_floor()` (sync.c ~1466), a continuous AIMD-style ramp (`g_sync_tension_probe_up/down/neutral_sps_per_s`, `_max_sps`, floor applied NEUTRAL-only) — this matches [[typed-tension-recovery-floor]]'s hardware-validated fix exactly, and that same memory file records "decaying-floor... FAILED+removed" as one of the REJECTED pivots that came *before* the AIMD probe shipped. The spec appears to describe the rejected design as if it were current — not a wording nit like the first 6 findings, an entire requirement section describing dead code. No scenario built (a step-up/type-D attempt just pinned the buffer at the TENSION rail from excess demand, not informative about touch-burst-collapse; needs more careful demand tuning than spent here). Requirement 2 (quiet `SYNC_MIN_RATE` default, slow-drift via reserve bias) checked out fine: 682 sps ≈ 100mm/min at this board's gearing, matches memory's prior "quiet default SYNC_MIN_RATE 100" note.
- `sync-refactor` (task 12, partial — resumed after a fresh session, task list carried the full context with zero conversation replay needed): 30 requirements, not 60 (60 was scenario count). Skimmed all titles: majority Klipper-sidecar/UDS/analyzer/protocol-rename scope, out of `flare_sim`'s reach. Of the runtime-relevant minority: "Type-D standalone buffer control is a hysteretic relay" already covered (`RelayFallbackTests`); "Normal switch contact does not trigger FAULT_HOLD" intent already confirmed by the motion-safety dead-code finding; added `test_tension_feeds_compression_backs_off` (`SyncRefactorTests`) for "Sync control polarity" (TENSION feeds materially more than COMPRESSION, both sensor types — `steady`/type-D, `burst`/type-P since `steady` never reaches COMPRESSION for type-P in a reasonable window); "Pin-to-state decode is verified non-inverted" is NOT sim-testable (sim injection bypasses the physical-pin-to-logical-signal mapping the requirement is actually about, that lives in unlinked main.c); "Type-D estimator does not hard-overwrite" not attempted (needs more setup than time allowed).
  Investigated but left INCONCLUSIVE (not a 9th finding — avoided overclaiming): "Type-D compression relief is overfill-budgeted" claims RELIEF_PAUSE fires within a small (~3mm) budget rather than the multi-second dwell timer. The dwell path (`sync_check_continuous_compression`) is gated on `g_sync_auto_started`, which the sim's `start_sync_active` shortcut never sets (bypasses the organic auto-engage path) — so no scenario built this way can even reach the code under test. `idle_zero`/type-D sustains COMPRESSION for a full 60s run with zero RELIEF_PAUSE events, but that's a sim gap, not evidence against the spec claim. Side-finding (informational, not a mismatch): the overfill-budget global (`g_sync_relieve_effort_mm` vs `g_sync_compression_drain_budget_mm`) actually gates a *different* mechanism (`sync_type_d_compression_drain_target()` — a partial "drain" feed rate during active-demand compression), not RELIEF_PAUSE entry at all. Resume: make the sim reach `g_sync_auto_started=true` via the organic auto-engage path (or set it directly for this scenario class) before re-attempting.
  Lesson reinforced: this session almost mis-attributed the auto_started gating gap as a 9th spec/code mismatch before double-checking why `idle_zero` never fired RELIEF_PAUSE — turned out to be a sim methodology limitation, not a firmware fact. Same "verify twice before claiming a defect" discipline as the fault-hold-recovery false alarm two sessions ago.
  `psf-type-p-sensor` (48 scenarios) still fully deferred — not opened this pass either, time went to sync-refactor instead.
- `psf-type-p-sensor` (task 11, done — net-new subset built, rest triaged/documented): read all 16 requirements/48 scenarios in full. Heavy overlap confirmed with existing type-P coverage: "Type-P Relief-Pause Auto-Recovery"'s "recovers under demand" = `sem_relief_pause_lifecycle` (already `sensor_type=p`); "Hard Catch and Print-Stop Detection"'s two `PSF_WALL_SAT_MS` saturation scenarios = the same `sem_relief_pause_lifecycle`/`sem_fault_hold_standalone_recovery` pair; "Type-P Fault Timers Scoped to Active Sync"'s "does not instantly re-fault" = `sem_fault_hold_standalone_recovery`'s already-documented ~1.5s re-entry cadence. Out of scope: PSF Endpoint Calibration/BUF_GOAL/BUF_RANGE-BUF_INVERT (protocol.c, unlinked); Continuous Extruder Estimate/Filtered Derivative/Gradual PD/Soft Walls (internal-state shape requirements, not in the CSV trace, effects already implicit in existing type-P scenarios); Type-P Feed Quality and Reliable Stabilize (spec's own words: "measured against a real print, not isolated bench bursts").
  Net-new: "Type-P Stabilize Rail Breakaway" was untested by anything (no prior scenario ever called `BS`/boot-stabilize under type-P). Added a `bs_request_at_ms` scenario trigger (mirrors `bl_arm_at_ms`'s pattern) calling the real `buffer_stabilize_request()` (the actual `BS` command entry point, confirmed via `protocol.c:1721`). Built `sem_psf_stab_rail_breakaway` (DEMAND_IDLE_ZERO pins the buffer at the tension rail with sync inactive by t=1500, BS at t=2000, stab motor's own drive moves the plant off the rail: `BUF_STAB:DONE` at t=3740, well inside the 3000ms `PSF_STAB_RAIL_BREAK_MS` cap — confirms the stagnation guard does NOT abort early while saturated) and `sem_psf_stab_rail_break_timeout` (same setup but `feed_gain` zeroed at the BS trigger models an uncoupled/jammed lane — the stab motor commands a rate but the plant never moves, so `BUF_STAB:STAGNANT_TIMEOUT` fires at exactly t=5000 = 2000+3000, confirming the cap itself). Both pass under both sensor types, no new plant/architecture work needed (motor-level fallback from the earlier BL work already covers stabilize's direct `lane->m` drive). No 9th finding this pass — implementation matches spec cleanly.
  Left uncovered (documented reasons in tasks.md 11.1, not silently dropped): relief-pause's transition-while-pinned/static-rest sub-scenarios (need precise velocity/position staging beyond current demand profiles); Tension Refill Snap's exact snap-then-settle mechanic (partially implicit in `SyncRefactorTests`, not isolated); "Normal extrude does not fault on engagement" (same `g_sync_auto_started` organic-engage gating blocker as sync-refactor's open item).
- `psf-type-p-sensor`, follow-up pass (user said "keep chasing" after the first pass): closed 2 of the 3 previously-open gaps.
  1. `sync_fast_brake` reversible-brake path ("Hard Catch and Print-Stop Detection"): needed NO new scenario — `retract`/type-P (brief 500ms pulse) already is "Slowdown recovers" (settles at 7.5mm compression, short of the 12.5mm rail, sync stays ACTIVE, no RELIEF_PAUSE/FAULT_HOLD); `long_retract`/type-P (sustained 200mm/s) already is "Real stop confirmed" (saturates and stays pinned, `SYNC,RELIEF_PAUSE` fires). Both existing scenarios, just never asserted against under type-P before. Added to `PsfTypePSensorTests`.
  2. "Type-P Unload Uses No Position-Based Over-Tension Guard": built. New `ul_start_at_ms`/`ul_target_lane` trigger calling the real `lane_start(..., TASK_UNLOAD, ...)` (mirrors `protocol.c`'s `start_manual_unload_lane`). Two scenarios: `sem_psf_unload_normal` (OUT clears mid-retract -> `UNLOADED`, both sensor types, no guard interference) and `sem_psf_unload_stuck` (OUT held present the whole run -> type-D's `UNLOAD_TENSION_BLOCK` dwell fires `UNLOAD_BLOCKED` at t=6200; type-P never blocks in the same window).
     Two real gotchas found empirically along the way, both by watching the sim not matching the comment I'd written, not by inspection:
     - My own prior memory note "sim boot default sensor state: in=true, out=false" was WRONG/stale — `sim_main.c` actually sets BOTH `in_sw`/`out_sw` `.stable=true` for both lanes at boot. First scenario attempt (OUT never touched, assuming it started false) sat inert for the whole run since OUT never cleared — fixed by adding an explicit `switch_script` clearing L1_OUT 1000ms into the retract (the same proven pattern as the `runout` scenario).
     - The type-D tension-block guard has a "both lanes' OUT present" double-load-recovery exception (`motion.c:378`) that the corrected boot default (both true) silently satisfies on its own — `UNLOAD_BLOCKED` never fired even with L1_OUT forced true until L2's OUT was also forced false.
     Deliberately did NOT chase the eventual type-P `UNLOAD_TIMEOUT`/`UNLOAD_MAX` distance fallback: confirmed empirically it collides with the generic 20s saturation invariant (the buffer's ~12.5mm physical rail saturates in ~1s; `UNLOAD_MAX`, 3000mm default, operates on the whole filament path, three orders of magnitude larger scale) — the guard's *absence* is the testable claim from the spec, not the eventual timeout, so left at that.
  Still open: relief-pause's transition-while-pinned/static-rest sub-cases (needs precise velocity/position staging beyond current demand profiles).
- User said "keep chasing" the two `g_sync_auto_started`-blocked items (psf-type-p-sensor's "Normal extrude does not fault on engagement" + sync-refactor's "Type-D compression relief is overfill-budgeted"). Found and applied the actual fix: `sync_tick_auto_start_stop` (sync.c:1319) — the real organic auto-engage path — was never unreachable, the sim just never staged its preconditions correctly. It needs `g_auto_mode=true` (scenario field already existed, just never set true anywhere), sync OFF at start, and **only one lane's OUT sensor present** — the function has a `!both_loaded` guard (both-OUT-present = double-load manual-recovery state, auto-start intentionally suppressed) that the sim's boot default (BOTH lanes' OUT true — see the boot-default correction two entries up) silently satisfies, blocking organic engage entirely until one lane's OUT is explicitly forced false via `switch_script`. Confirmed empirically: with that staged correctly, `SYNC,AUTO_START` fires cleanly and organically for both sensor types, `g_sync_auto_started` genuinely true afterward.
  1. psf-type-p-sensor "Normal extrude does not fault on engagement": CONFIRMED HOLDS. `sem_psf_no_fault_on_idle_engagement` — 8s idle (sync OFF, demand=0) then demand kicks in and organically engages. Zero `FAULT_HOLD` in a 16s run, both sensor types. Root cause double-checked by grep: NOTHING in the current codebase sets `g_sync_tension_pin_since_ms` while sync is OFF (the only writers are inside `sync_rearm_active`/the type-P fast-brake path/this same auto-start function — all sync-active-only) — the dwell timer structurally can't go stale during idle, and every activation path resets it fresh anyway (`sync.c:1078/1376`). The requirement's guarantee holds by construction, not by luck.
  2. sync-refactor "Type-D compression relief is overfill-budgeted": RESOLVED, upgraded from inconclusive to a confirmed 9th spec/code mismatch. `sem_sync_overfill_budget_probe` (reuses `idle_zero`'s demand profile, organically engaged) shows `RELIEF_PAUSE` firing at t=8280 from a compression onset at ~t=3500 — a ~4780ms dwell, matching `CONF_SYNC_AUTO_STOP_MS` (5000ms) almost exactly. This is a TIME-based dwell trigger (`sync_check_continuous_compression`, gated on feed already at the compression floor), not the small (~3mm) distance-based "overfill budget" the spec's wording implies. The overfill-budget globals (`g_sync_relieve_effort_mm`/`g_sync_compression_drain_budget_mm`) are real and do exist, but — confirmed by reading `sync_type_d_compression_drain_target()` — gate a completely different mechanism (a partial-feed "drain" rate during active-demand compression), never RELIEF_PAUSE entry. Added `test_compression_relief_is_dwell_timed_not_distance_budgeted` (`SyncRefactorTests`) asserting the firing lands in a 7-9s window from scenario start.
  Both fixes required zero firmware changes and zero new scenario-struct fields — pure staging of existing `auto_mode`/`switch_script` scenario mechanics. 36/36 python tests green after, firmware diff still empty.
- Real type-P rig confirmed H6 (audit-reliability-fixes task 12.4, HW: box now checked) via a `/telemetry` SSE capture (`reload_mode=1`, `buf_sensor_type=1`): `RUNOUT,2` -> `RELOAD:SWITCHING,2->1` same tick, `RELOAD:JOINING,1` ~17.6s later, `RELOAD:LOADED,1` ~3.8s after that — zero `SYNC:FAULT_HOLD`, zero `FOLLOW_JAM` in the whole capture. Matches `reload_genuine_runout_escalation`'s sim assertions closely (same event names, same zero-fault-hold outcome). Bonus: post-load organic recovery (`cannot_refill` -> `RELIEF_PAUSE` -> `AUTO_START`, ~4s apart) also observed live, matching the `sync_tick_auto_start_stop` re-arm mechanism the sim only just validated two sessions prior — real hardware corroborates the sim fix. `RELOAD_MODE=0` still not run on rig (open gap, same as before).
- `ARCHITECTURE_BRIEF.md` self-audit (untracked scratch doc, not authored this session but reviewed on request): triaged its findings, user picked 2 to address. (a) Wired `.github/workflows/ci.yml` running `scripts/validate_regression.py` on push/PR (fetches pico-sdk via `PICO_SDK_FETCH_FROM_GIT=ON`, cached on `build_local/_deps`) — closes the brief's "no CI/CD" gap. Found the local gate script itself was NOT actually green end-to-end before this: 3 pre-existing `ruff` F841 findings in `flare_daemon.py` (dead `sps`/`feed_rate`/`rev_rate` reads, fixed by deletion) and 7 failing `scripts/test_flare_mmu_status.py` assertions — the latter traced (confirmed via `git stash`, present on origin/main untouched) to the `94e27a2` "strip synthetic-mm tip animation from mmu mock" commit: the mock's `get_status()` intentionally stopped computing `filament_position`/`bowden_progress`/`filament_pos==3` (IN_BOWDEN) and moved `unload_completed` from a `get_status()` side-effect to `_update_phase()`, but the test file was never updated and still asserted the removed behavior. Not a production bug — rewrote the stale test blocks (`#3`/`#4`) to assert the CURRENT intentional behavior (discrete `filament_pos` landmarks {0,4,10} only, `filament_position` always 0.0, `unload_completed` latched via `_update_phase("UNLOAD_WAIT_CUT",...)` not raw field pokes). `python3 scripts/validate_regression.py` now passes clean for the first time. (b) Added a flash-wear visibility counter (change `flash-wear-visibility`): `g_flash_erase_count` persisted field, incremented once per `settings_save()` before that save's erase+program, restored on load, seeded to 0 by `settings_defaults()` (including on `RS:` — same rule every other field follows, no special-casing). `GET:FLASH_ERASE_COUNT` + one-time `EV:FLASH:WEAR_WARNING` at `FLASH_WEAR_WARN_THRESHOLD`=80000. `SETTINGS_VERSION` bumped 60->61 (unavoidable given this repo's no-migration model). Real firmware (`build_local`) and sim (`build_sim`) both rebuild clean, `test_settings_parity.py` passes (field saved+loaded+defaulted), full local gate green end-to-end. Not addressed (user's choice, bigger lifts): the daemon's no-auth `POST /cmd`/CORS `*` itself (highest-severity finding in the brief, still genuinely open — needs token auth or a `/cmd` allowlist), settings TLV/migration format, the 161-globals/tick-order coupling (already being actively mitigated by `tests/host/`, not "fixed" outright).
- Follow-up: user asked whether the daemon `0.0.0.0` default was tracked in specs — yes, but stale. `openspec/changes/audit-hardening-fixes/` (still open, not archived) has task 4.1 checked `[x]` and a `daemon-klipper-mirror` spec delta both claiming a `127.0.0.1` loopback default shipped and stuck. It did ship once, then commit `4ae1a19` (2026-06-19) reverted it: the loopback default broke the LAN-reachable WebUI (the whole point of the dashboard), and worse, `install_daemon.py`'s systemd unit template had no `--host` arg at all, so reinstalling silently dropped ANY manual override (in either direction) — the "safe" default gave no durable protection anyway. That exact risk was predicted in this same change's own design.md ("[Daemon bind flip breaks remote WebUI] → explicit `--host 0.0.0.0` restores") but the escape hatch didn't survive a reinstall, so it broke in practice regardless.
  User then asked to fix what's addressable without a breaking change (i.e. NOT flip the default back to loopback — that's the exact thing already tried and reverted for a real reason). Two things done: (1) reconciled the stale tracking — task 4.1 annotated with the full revert story instead of a misleading `[x]`, the spec delta requirement marked "SUPERSEDED, do not fold into the live spec" (kept verbatim below the marker for audit trail), proposal.md's S1 line annotated — all doc-only, zero behavior change. (2) Fixed the actual root cause of why an operator's manual override didn't survive a reinstall: `install_daemon.py`'s `step_systemd_unit()` now parses any existing ExecStart's extra args (e.g. a manual `--host 127.0.0.1`) before regenerating the unit from the template, and re-appends them — so a deliberate loopback choice is now durable across reinstalls, without touching the default for anyone who never customized it. Added `scripts/test_install_daemon.py` (7 tests, monkeypatches `SERVICE_TEMPLATE`/`SERVICE_DEST` to a tmp dir, no real systemd/root interaction) covering: plain install (no extra args), reinstall preserving a manual `--host` override, reinstall with nothing to preserve. All green, `ruff`/`py_compile` clean, `openspec validate audit-hardening-fixes --strict` passes (had to reorder the superseded-requirement's STATUS note to come AFTER the SHALL sentence, not before — same "checks only the first line for SHALL/MUST" validator quirk noted earlier this project).
- 9th real firmware bug found via a SECOND real-rig capture (2026-07-27, same session): on that run RELOAD never triggered at all on a genuine runout. `/telemetry` showed `FAULT_HOLD_RECOVERY -> AUTO_START -> (re-saturate ~1s) -> FAULT_HOLD` looping 10+ times, ~6.4s period, `reload_mode=1`, zero `RUNOUT`/`RELOAD` ever. Root cause (via superpowers:systematic-debugging, confirmed in code not guessed): `sync_tick()` runs `sync_tick_gated_checks()` first; if it returns true the tick returns immediately. `sync_tick_gated_checks` calls `sync_tick_type_p_rail_guard()` first, which fault-holds on `CONF_PSF_WALL_SAT_MS` (1000ms) rail saturation — H6's RELOAD escalation (`sync_check_tension_dwell_and_ramp`, gated on `FLARE_INT_SYNC_TENSION_DWELL_STOP_MS`=6000ms) lives in `sync_tick_calculate_target`, called LATER, and never reached once gated_checks already returned. Dwell timer resets to 0 on every rearm (the psf-stale-fault-timers fix), so a fast/complete runout that saturates the rail in ~1s always re-triggers the 1s rail-guard path before the 6s dwell timer can ever fire — H6 was permanently unreachable for any FAST runout, only ever worked for slow/moderate demand (exactly why `reload_genuine_runout_escalation`'s scenario comment used artificially-low 1mm/s demand and flagged "worth confirming on rig at realistic print speeds" — now confirmed). Fixed (change `psf-runout-escalation-race-fix`): extracted the runout-escalation check into shared helper `sync_try_runout_escalation()`, called from BOTH fault-hold entry paths (the fast rail-guard path and the slow dwell path) instead of just the dwell path. New sim scenario `reload_fast_runout_rail_guard_race` (high 20mm/s demand) reproduces the bug RED against pre-fix firmware (656 FAULT_HOLD events, zero RUNOUT, TDD per systematic-debugging phase 4), then GREEN after the fix (RUNOUT/RELOAD:SWITCHING fire on the exact same tick the fault-hold used to). `reload_genuine_runout_escalation` (slow path) unaffected — zero regression. 37/37 tests green, `git diff --name-only main -- firmware/` shows only `sync.c`, `build_local` (dev-tuning superset) clean.
