---
phase: 14-klipper-mmu-status-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/flare_daemon.py
  - klipper/mmu.py
  - scripts/test_flare_mmu_status.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-klipper-status-parity-flowguard-dict
  - REQ-klipper-status-parity-missing-keys

estimate:
  tokens: 88000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "printer.mmu.flowguard is present with enabled/active/trigger/reason/level/max_clog/max_tangle keys; level is 0.0 and active is False whenever sync_drive is off, regardless of dwell timers"
    - "printer.mmu.flowguard.level moves toward +1.0 (clog) as CT: compression dwell approaches 5000ms and toward -1.0 (tangle) as TT: tension dwell approaches 6000ms, using only fields already on the wire (no firmware change)"
    - "Every HH v4 printer.mmu key in the RESEARCH.md diff table that has a FLARE source (sync_drive, is_paused, reason_for_pause, sync_feedback_flow_rate, has_bypass, unit, operation, drying_state, espooler, espooler_active, extruder_filament_remaining, filament_direction, gate_temperature, grip, last_tool, next_tool, slicer_tool_map, endless_spool_enabled, endless_spool_groups, toolchange_purge_volume, encoder, clog_detection_enabled) exists with the correct type after a representative SET_MMU push"
    - "get_status() performs no network or serial I/O — every new key reads only self.* attributes or cheap arithmetic over them"
  artifacts:
    - klipper/mmu.py (get_status() flowguard dict + 24 new/renamed keys total across all three tasks; cmd_SET_MMU FLOWGUARD_*/SYNC_DRIVE/IS_PAUSED/REASON_FOR_PAUSE/BASELINE_SPS params; __init__ defaults)
    - scripts/flare_daemon.py (_flowguard_level, TT:/CT: parsing, sync_state/sync_drive-derived mirroring, extended _MMU_RECONCILE_* tables with nested-dict support)
    - scripts/test_flare_mmu_status.py (flowguard tracer checks + extended reconcile-fields-dict block)
    - .planning/REQUIREMENTS.md (six REQ-klipper-status-parity-* entries registered)
  key_links:
    - "parse_status_line TT:/CT: -> status_cache[tension_dwell_ms/compression_dwell_ms] -> klipper_syncer._flowguard_level() -> SET_MMU FLOWGUARD_* -> cmd_SET_MMU -> get_status()['flowguard']"
    - "status_cache[sync_state] == SYNC_FAULT_HOLD(4) -> klipper_syncer is_paused/reason_for_pause -> SET_MMU IS_PAUSED/REASON_FOR_PAUSE -> get_status()['is_paused'/'reason_for_pause']"
    - "_MMU_RECONCILE_STATUS_KEYS now maps some keys to (dict_key, nested_key) tuples; _format_mmu_reconcile_value must resolve both scalar and nested forms or the 10s reconcile silently stops detecting drift for flowguard fields"
  manual:
    - "HW: FlowGuard meter renders and visibly moves in a live Fluidd and Mainsail MMU panel with sync active (14-VALIDATION.md Manual-Only Verifications) — not auto-checkable from this plan"
---

<objective>
Wire FLARE's dwell-time fault-approach telemetry (`TT:`/`CT:` fields already on the wire, `firmware/src/protocol_status.c:64-80`) through to a Happy-Hare-shaped `printer.mmu.flowguard` dict, and fill in every `printer.mmu` status key Fluidd/Mainsail `develop` reads that FLARE is currently missing a FLARE-side source for. No firmware changes. This is Wave 1 of Phase 14 — Plan 02 (Wave 2) adds command stubs, the full action-string vocabulary, `happy_hare_version`, and the schema-presence test.

Purpose: Phase 14 Success Criteria 1 (FlowGuard) and the FLARE-sourced half of Criterion 2 (missing keys) — see `.planning/ROADMAP.md` "### Phase 14:".
Output: `klipper/mmu.py` and `scripts/flare_daemon.py` extended in place; `scripts/test_flare_mmu_status.py` extended with tracer + reconcile coverage; the six phase REQ IDs registered in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
</objective>

<execution_context>
@~/.claude-personal/gsd-core/workflows/execute-plan.md
@~/.claude-personal/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-klipper-mmu-status-parity/14-RESEARCH.md
@.planning/phases/14-klipper-mmu-status-parity/14-PATTERNS.md
@.planning/phases/14-klipper-mmu-status-parity/14-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — FlowGuard dwell-to-status slice (TT:/CT: → daemon level → SET_MMU → mmu.py → test)</name>
  <files>scripts/flare_daemon.py, klipper/mmu.py, scripts/test_flare_mmu_status.py, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    scripts/flare_daemon.py:511-598 (parse_status_line elif chain, inside a try/except ValueError that already guards every branch);
    scripts/flare_daemon.py:599-621 (keys_to_check wake-gate tuple and status_cache.update/trigger_klipper_sync);
    scripts/flare_daemon.py:1210-1272 (_MMU_RECONCILE_STATUS_KEYS/_MMU_RECONCILE_FLOATS/_MMU_RECONCILE_STRINGS);
    scripts/flare_daemon.py:1298-1327 (_format_mmu_reconcile_value, _mmu_status_matches_fields);
    scripts/flare_daemon.py:1343-1358 (_derive_action — insertion anchor, do not modify this task);
    scripts/flare_daemon.py:1379-1394 (klipper_syncer local-state init, e.g. last_pushed_fields — insertion anchor for new high-water-mark locals);
    scripts/flare_daemon.py:1428-1442 (change-detection keys list);
    scripts/flare_daemon.py:1547-1582 (fields dict — insertion anchor for FLOWGUARD_* entries, after BYPASS);
    klipper/mmu.py:57-103 (__init__ attribute defaults — insertion anchor);
    klipper/mmu.py:206-230 (cmd_SET_MMU action/tc_state block — insertion anchor);
    klipper/mmu.py:1427-1497 (get_status return dict — insertion anchor, after sync_feedback_enabled or at end);
    scripts/test_flare_mmu_status.py:1-30 (imports/fakes), :225-300 (existing reconcile block — extend, do not replace);
    14-RESEARCH.md sections "FlowGuard — exact HH v4 dict shape, semantics, and range" and "ST: fields available for flowguard.level — no firmware change needed" (exact dict shape, sign convention, MANUAL.md-documented defaults 6000ms/5000ms);
    .planning/REQUIREMENTS.md:372-399 (REQ-daemon-klipper-mirror-* entries — format to replicate).
  </read_first>
  <behavior>
    - `_flowguard_level(sync_active=True, tension_dwell_ms=6000, compression_dwell_ms=0)` returns `(-1.0, "tangle")` (tension pinned at the 6000ms MANUAL.md default saturates the tension side).
    - `_flowguard_level(sync_active=True, tension_dwell_ms=0, compression_dwell_ms=2500)` returns `(0.5, "")` (halfway to the 5000ms compression stop, not yet tripped).
    - `_flowguard_level(sync_active=False, tension_dwell_ms=6000, compression_dwell_ms=0)` returns `(0.0, "")` — sync inactive forces 0 regardless of dwell (Success Criterion 1).
    - After `parse_status_line("OK:LN:1,TC:IDLE,L1T:IDLE,L2T:IDLE,TT:1234,CT:0")`: `status_cache["tension_dwell_ms"] == 1234`, `status_cache["compression_dwell_ms"] == 0`, and `klipper_sync_event.is_set()` is True (dwell fields are in the wake-gate tuple, matching the existing `test_sync_event_triggers_on_discrete_change...` pattern in `scripts/test_flare_daemon_stats.py`).
    - After `m.cmd_SET_MMU(FakeGcmd({"FLOWGUARD_ENABLED": 1, "FLOWGUARD_ACTIVE": 1, "FLOWGUARD_TRIGGER": "'tangle'", "FLOWGUARD_REASON": "'Tension dwell approaching trip'", "FLOWGUARD_LEVEL": -1.0, "FLOWGUARD_MAX_CLOG": 0.0, "FLOWGUARD_MAX_TANGLE": -1.0}))`, `m.get_status(0)["flowguard"]` equals exactly `{"enabled": True, "active": True, "trigger": "tangle", "reason": "Tension dwell approaching trip", "level": -1.0, "max_clog": 0.0, "max_tangle": -1.0}`.
  </behavior>
  <action>
    Register the six phase requirement IDs verbatim from the planning brief (REQ-klipper-status-parity-flowguard-dict, -missing-keys, -hh-version, -command-stubs, -action-strings, -schema-test) as new `### REQ-...` entries in `.planning/REQUIREMENTS.md`, following the exact `### REQ-ID` / `- **Source**:` / `- **Description**:` format of the existing `REQ-daemon-klipper-mirror-*` entries; set Source to `.planning/phases/14-klipper-mmu-status-parity/14-RESEARCH.md` for all six (no spec.md exists for this phase). Replace the `**Requirements**: TBD (derive from research §1.3, §5, §6, §7)` line under `### Phase 14:` in `.planning/ROADMAP.md` with the six IDs, comma-separated.

    In `scripts/flare_daemon.py`, add two `elif` branches to `parse_status_line`'s existing try-guarded chain, immediately after the `UL_RATE` branch: `key == "TT"` sets `new_data["tension_dwell_ms"] = int(val)`; `key == "CT"` sets `new_data["compression_dwell_ms"] = int(val)`. Add `"tension_dwell_ms"` and `"compression_dwell_ms"` to the `keys_to_check` tuple so a dwell-timer edge wakes `klipper_syncer` promptly instead of waiting for the 10s reconcile (per RESEARCH.md Pitfall 4's "dwell-timer edges that actually matter"); add the same two names to `klipper_syncer`'s own change-detection `keys` list.

    Add two module-level constants near `_derive_action`: `_FLOWGUARD_TENSION_STOP_MS = 6000` (MANUAL.md `SYNC_TENSION_STOP_MS` default; the live `GET:` is `FLARE_DEV_TUNING`-gated per RESEARCH.md Pitfall 3, so hardcode rather than query it) and `_FLOWGUARD_COMPRESSION_STOP_MS = 5000` (`tune.h CONF_SYNC_AUTO_STOP_MS`). Add a pure function `_flowguard_level(sync_active, tension_dwell_ms, compression_dwell_ms, tension_stop_ms=_FLOWGUARD_TENSION_STOP_MS, compression_stop_ms=_FLOWGUARD_COMPRESSION_STOP_MS)` implementing exactly the Behavior cases above (sign convention: positive = compression/"clog", negative = tension/"tangle", matching HH's `mmu_sync_controller.py:807-905`).

    In `klipper_syncer`, initialize two persistent locals before the `while True:` loop, alongside `last_pushed_fields`: `flowguard_max_clog = 0.0` and `flowguard_max_tangle = 0.0`. Inside the loop, after `tc_state`/`bypass` are read from `state`: compute `sync_active = bool(state.get("sync_drive", False))`, `tension_dwell_ms = state.get("tension_dwell_ms", 0)`, `compression_dwell_ms = state.get("compression_dwell_ms", 0)`, `flowguard_level, flowguard_trigger = _flowguard_level(sync_active, tension_dwell_ms, compression_dwell_ms)`, `flowguard_active = sync_active and flowguard_level != 0.0`, `flowguard_enabled = sync_feedback_enabled` (reuse the existing hardcoded-1 local), and `flowguard_reason` = `"Tension dwell approaching trip"` when `flowguard_trigger == "tangle"`, `"Compression dwell approaching trip"` when `"clog"`, else `""`. Update the high-water marks: if `not sync_active`, reset both to `0.0`; elif `flowguard_level > 0`, `flowguard_max_clog = max(flowguard_max_clog, flowguard_level)`; elif `flowguard_level < 0`, `flowguard_max_tangle = min(flowguard_max_tangle, flowguard_level)`. Add seven entries to the `fields` dict after `"BYPASS"`: `FLOWGUARD_ENABLED`, `FLOWGUARD_ACTIVE` (as `"1"`/`"0"`), `FLOWGUARD_TRIGGER`, `FLOWGUARD_REASON` (quoted strings), `FLOWGUARD_LEVEL`, `FLOWGUARD_MAX_CLOG`, `FLOWGUARD_MAX_TANGLE` (formatted `f"{v:.3f}"`).

    Extend `_MMU_RECONCILE_STATUS_KEYS` with `FLOWGUARD_ENABLED`/`FLOWGUARD_ACTIVE`/`FLOWGUARD_TRIGGER`/`FLOWGUARD_REASON`/`FLOWGUARD_LEVEL`/`FLOWGUARD_MAX_CLOG`/`FLOWGUARD_MAX_TANGLE` mapped to 2-tuples `("flowguard", "enabled")` etc. (the `flowguard` sub-dict's own key), since `flowguard` is nested unlike every existing reconciled key. Extend `_format_mmu_reconcile_value` so that when `_MMU_RECONCILE_STATUS_KEYS.get(key)` is a tuple, it walks `status[status_key[0]][status_key[1]]` (returning `None` if either level is missing/not a dict, preserving today's scalar-key behavior unchanged in the `else` branch). Add `FLOWGUARD_LEVEL`/`FLOWGUARD_MAX_CLOG`/`FLOWGUARD_MAX_TANGLE` to `_MMU_RECONCILE_FLOATS` with precision 3; add `FLOWGUARD_TRIGGER`/`FLOWGUARD_REASON` to `_MMU_RECONCILE_STRINGS`.

    In `klipper/mmu.py` `__init__`, add seven defaults after `self.sync_feedback_enabled = False`: `self.flowguard_enabled = False`, `self.flowguard_active = False`, `self.flowguard_trigger = ""`, `self.flowguard_reason = ""`, `self.flowguard_level = 0.0`, `self.flowguard_max_clog = 0.0`, `self.flowguard_max_tangle = 0.0`. In `cmd_SET_MMU`, after the `self.sync_feedback_enabled = ...` line, add the matching `gcmd.get_int`/`gcmd.get_float`/`gcmd.get(...).strip("'\"")` reads for all seven (following the exact defaults-preserved-when-absent idiom already used for every other field). In `get_status()`, add a `'flowguard': {...}` entry (place it after `'sync_feedback_enabled': self.sync_feedback_enabled,`) assembling the 7-key dict from `self.flowguard_*` — this method still performs zero I/O (Pitfall 1).

    Extend `scripts/test_flare_mmu_status.py`'s reconcile block (`:225-300`) so its `fields` dict also includes the seven `FLOWGUARD_*` keys (formatted the same way `SET_MMU` would format them), proving `_mmu_status_matches_fields` correctly resolves the new nested-tuple reconcile keys. Add new `check()` calls implementing every Behavior case above (pure-function saturation/inactive-zero cases, TT/CT parse + wake-gate case, and the flowguard-dict round-trip case).
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/*.py klipper/*.py && python3 -m unittest scripts.test_flare_mmu_status -v</automated>
    <fails_when>non-zero exit code, or "FAILED" appears in the unittest output</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains all six `### REQ-klipper-status-parity-*` headings; `.planning/ROADMAP.md`'s Phase 14 `**Requirements**:` line no longer contains the word `TBD`.
    - `grep -c "'flowguard':" klipper/mmu.py` is `>= 1` (the get_status() nested dict).
    - `grep -c "elif key == \"TT\":" scripts/flare_daemon.py` and `grep -c "elif key == \"CT\":" scripts/flare_daemon.py` are each `>= 1`, and both sit textually before the function's closing `except ValueError:` line (i.e. inside the existing guarded chain — confirm via `grep -n`).
    - `python3 -c "import sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'klipper'); import types; sys.modules.setdefault('serial', types.SimpleNamespace()); import flare_daemon; print(flare_daemon._flowguard_level(False, 6000, 0))"` prints `(0.0, '')`.
    - Three-location registration, every one of the seven new `FLOWGUARD_*` fields checked individually (per RESEARCH.md Pitfall 4 / locked decision 5 — not a representative sample): `grep -n '"FLOWGUARD_ENABLED"\|"FLOWGUARD_ACTIVE"' scripts/flare_daemon.py` shows both names present in the `fields` dict assignment and in `_MMU_RECONCILE_STATUS_KEYS` (two locations each — these two are booleans and correctly do NOT appear in `_MMU_RECONCILE_FLOATS`/`_MMU_RECONCILE_STRINGS`, since `_format_mmu_reconcile_value`'s generic `isinstance(value, bool)` branch already formats them). `grep -n '"FLOWGUARD_TRIGGER"\|"FLOWGUARD_REASON"' scripts/flare_daemon.py` shows both names present in the `fields` dict, `_MMU_RECONCILE_STATUS_KEYS`, and `_MMU_RECONCILE_STRINGS` (three locations each). `grep -n '"FLOWGUARD_LEVEL"\|"FLOWGUARD_MAX_CLOG"\|"FLOWGUARD_MAX_TANGLE"' scripts/flare_daemon.py` shows all three names present in the `fields` dict, `_MMU_RECONCILE_STATUS_KEYS`, and `_MMU_RECONCILE_FLOATS` (three locations each).
    - `python3 -m unittest scripts.test_flare_mmu_status -v` reports `OK` on the last line.
  </acceptance_criteria>
  <done>All Behavior cases pass as automated checks; `flowguard` is reachable end-to-end from a raw `TT:`/`CT:` wire line through to `MMUMock.get_status()`; the six REQ IDs exist in REQUIREMENTS.md and ROADMAP.md no longer says TBD.</done>
  <reversibility rating="reversible">Purely additive parsing branches, a new pure function, and new dict/status keys; nothing existing is removed or renamed. Revert via git if the derivation formula needs rework.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daemon-mirrored keys — sync_drive, is_paused/reason_for_pause, sync_feedback_flow_rate</name>
  <files>scripts/flare_daemon.py, klipper/mmu.py, scripts/test_flare_mmu_status.py</files>
  <read_first>
    firmware/include/sync.h:6-12 (sync_state_t enum: SYNC_OFF=0, SYNC_ACTIVE=1, SYNC_RETRACT_ASSIST=2, SYNC_RELIEF_PAUSE=3, SYNC_FAULT_HOLD=4);
    scripts/flare_daemon.py:557-561 (SM:/ST: parsing — `sync_enabled`, `sync_drive`, `sync_state` already land in status_cache with no daemon change needed);
    scripts/flare_daemon.py:565-567 (BF: → `baseline_sps`, already parsed);
    scripts/flare_daemon.py:1466-1488 (klipper_syncer local computation block, insertion anchor — same region as Task 1's flowguard block);
    scripts/flare_daemon.py:1547-1582 (fields dict, insertion anchor, after the Task 1 FLOWGUARD_* entries);
    scripts/flare_daemon.py:1210-1272 (reconcile tables, insertion anchor);
    klipper/mmu.py:57-103 (__init__), :206-255 (cmd_SET_MMU), :1427-1497 (get_status) — same insertion anchors as Task 1, now appended after the flowguard block.
    14-RESEARCH.md "HH v4 printer.mmu schema diff table" rows for `sync_drive`, `is_paused`, `reason_for_pause`, `sync_feedback_flow_rate`; Open Question 2 ("is_paused/reason_for_pause semantics") and the orchestrator's minimum-bar decision: derive from existing daemon-known fault state, no new firmware state.
  </read_first>
  <behavior>
    - `status_cache["sync_state"] == 4` (SYNC_FAULT_HOLD) → the pushed `IS_PAUSED` field is `"1"` and `REASON_FOR_PAUSE` equals the quoted current `mmu_stats["last_error"]`; any other `sync_state` value → `IS_PAUSED` is `"0"` and `REASON_FOR_PAUSE` is `"''"`.
    - `status_cache["sync_drive"] == True` → pushed `SYNC_DRIVE` is `"1"`; `False` → `"0"`.
    - After `m.cmd_SET_MMU(FakeGcmd({"SPS": 100.0, "BASELINE_SPS": 50.0}))`, `m.get_status(0)["sync_feedback_flow_rate"] == 50.0` (`100 * min(1, 50/100)`); with `SPS=0`, `sync_feedback_flow_rate == 100.0` (guarded, no div-by-zero).
    - After `m.cmd_SET_MMU(FakeGcmd({"IS_PAUSED": 1, "REASON_FOR_PAUSE": "'ABORTED'"}))`, `m.get_status(0)["is_paused"] is True` and `["reason_for_pause"] == "ABORTED"`.
  </behavior>
  <action>
    Add a module-level constant `_SYNC_STATE_FAULT_HOLD = 4` (documented as `firmware/include/sync.h` `SYNC_FAULT_HOLD`, the 5th `sync_state_t` enum member) near `_flowguard_level`. In `klipper_syncer`, after the Task 1 flowguard block: compute `sync_state_val = state.get("sync_state", 0)`, `is_paused = 1 if sync_state_val == _SYNC_STATE_FAULT_HOLD else 0`, `reason_for_pause = f"'{st['last_error']}'" if is_paused else "''"` (reuse the existing `st = dict(mmu_stats)` snapshot already taken for `MMU_LAST_ERROR`), and `sync_drive_val = 1 if state.get("sync_drive", False) else 0`. Add `"sync_state"` and `"sync_enabled"` to both the `parse_status_line` `keys_to_check` tuple and `klipper_syncer`'s change-detection `keys` list (Task 1's anchor) so a fault-entry or sync-toggle edge wakes the syncer promptly. Compute `baseline_sps = state.get("baseline_sps", 0.0)` and add `"BASELINE_SPS": f"{baseline_sps:.3f}"` plus `"SYNC_DRIVE": str(sync_drive_val)`, `"IS_PAUSED": str(is_paused)`, `"REASON_FOR_PAUSE": reason_for_pause` to the `fields` dict.

    In `klipper/mmu.py` `__init__`, add `self.sync_drive = False`, `self.is_paused = False`, `self.reason_for_pause = ""`, `self.baseline_sps = 0.0`. In `cmd_SET_MMU`, add the matching `gcmd.get_int('SYNC_DRIVE', ...)`, `gcmd.get_int('IS_PAUSED', ...)`, `gcmd.get('REASON_FOR_PAUSE', ...).strip("'\"")`, `gcmd.get_float('BASELINE_SPS', ...)` reads, same defaults-preserved-when-absent idiom. In `get_status()`, add `'sync_drive': self.sync_drive`, `'is_paused': bool(self.is_paused)`, `'reason_for_pause': self.reason_for_pause`, and compute `sync_feedback_flow_rate = round(100.0 * min(1.0, self.baseline_sps / self.sps), 1) if self.sps > 0 else 100.0` then add `'sync_feedback_flow_rate': sync_feedback_flow_rate` (borrow-scan formula, `.planning/research/2026-09-11-happy-hare-borrow-scan.md:228-230`).

    Register the four new SET_MMU fields (`SYNC_DRIVE`, `IS_PAUSED`, `REASON_FOR_PAUSE`, `BASELINE_SPS`) in `_MMU_RECONCILE_STATUS_KEYS` (scalar keys this time: `"sync_drive"`, `"is_paused"`, `"reason_for_pause"`; `BASELINE_SPS` has no direct get_status() mirror key by itself — reconcile it against a derived read of `sync_feedback_flow_rate` is not exact, so instead reconcile `BASELINE_SPS` by adding a plain `self.baseline_sps`-backed key: expose it directly too by also adding `'baseline_sps': self.baseline_sps` to get_status() so the reconcile table can map `"BASELINE_SPS": "baseline_sps"` scalar, matching every other numeric field's pattern). Add `"BASELINE_SPS"` to `_MMU_RECONCILE_FLOATS` (precision 3) and `"REASON_FOR_PAUSE"` to `_MMU_RECONCILE_STRINGS`.

    Extend `scripts/test_flare_mmu_status.py`'s reconcile `fields` dict with `SYNC_DRIVE`, `IS_PAUSED`, `REASON_FOR_PAUSE`, `BASELINE_SPS`. Add `check()` calls for every Behavior case above.
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/*.py klipper/*.py && python3 -m unittest scripts.test_flare_mmu_status -v</automated>
    <fails_when>non-zero exit code, or "FAILED" appears in the unittest output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "_SYNC_STATE_FAULT_HOLD" scripts/flare_daemon.py` shows the constant defined as `4` on one line and referenced (compared against `sync_state_val`) on another.
    - `grep -c "'sync_feedback_flow_rate'" klipper/mmu.py` and `grep -c "'is_paused'"  klipper/mmu.py` are each `>= 1` (get_status key present).
    - Three-location registration, every one of the four new fields checked individually (per RESEARCH.md Pitfall 4 / locked decision 5 — not a representative sample): `grep -n '"SYNC_DRIVE"\|"IS_PAUSED"' scripts/flare_daemon.py` shows both names present in the `fields` dict assignment and in `_MMU_RECONCILE_STATUS_KEYS` (two locations each — these two are booleans and correctly do NOT appear in `_MMU_RECONCILE_FLOATS`/`_MMU_RECONCILE_STRINGS`, handled by the generic bool branch in `_format_mmu_reconcile_value`). `grep -n '"REASON_FOR_PAUSE"' scripts/flare_daemon.py` shows it present in the `fields` dict, `_MMU_RECONCILE_STATUS_KEYS`, and `_MMU_RECONCILE_STRINGS` (three locations). `grep -n '"BASELINE_SPS"' scripts/flare_daemon.py` shows it present in the `fields` dict, `_MMU_RECONCILE_STATUS_KEYS`, and `_MMU_RECONCILE_FLOATS` (three locations).
    - `python3 -m unittest scripts.test_flare_mmu_status -v` reports `OK` on the last line.
  </acceptance_criteria>
  <done>sync_drive, is_paused, reason_for_pause, and sync_feedback_flow_rate are live daemon-mirrored keys with the correct type and value under both the normal and fault-hold cases; no new firmware state was added.</done>
  <reversibility rating="reversible">Additive mirrored fields derived from existing status_cache signals; revert via git.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Static/derived missing keys + clog_detection_enabled rename fix</name>
  <files>klipper/mmu.py, scripts/test_flare_mmu_status.py</files>
  <read_first>
    klipper/mmu.py:1427-1497 (get_status return dict — final insertion point, after Task 1/2 additions);
    klipper/mmu.py:1379-1399 (`self.current_phase` values "idle"/"load"/"unload", already used at :1394 for the bypass-cascade check);
    14-RESEARCH.md "HH v4 printer.mmu schema diff table" — rows for `has_bypass`, `unit`, `operation`, `drying_state`, `espooler`, `espooler_active`, `extruder_filament_remaining`, `filament_direction`, `gate_temperature`, `grip`, `last_tool`/`next_tool`, `slicer_tool_map`, `endless_spool_enabled`/`endless_spool_groups`, `toolchange_purge_volume`, `encoder`, `clog_detection_enabled` (and Pitfall 2: `encoder: None` is a deliberate literal key, not an omission);
    14-PATTERNS.md "For static/no-hardware keys... copy this literal style: hardcoded Python value inline in the return dict".
  </read_first>
  <action>
    Add these literal/derived keys to `klipper/mmu.py` `get_status()`'s return dict (no new `self.*` attributes needed — every value is a static literal or arithmetic over an attribute that already exists): `'has_bypass': True` (top-level; distinct from the existing `mmu_machine.unit_0.has_bypass`); `'unit': 0`; `'operation': ''`; `'drying_state': ''`; `'espooler': [''] * self.num_gates`; `'espooler_active': ''`; `'extruder_filament_remaining': 0.0`; `'filament_direction': 1 if self.current_phase == "load" else (-1 if self.current_phase == "unload" else 0)`; `'gate_temperature': [0] * self.num_gates`; `'grip': "Gripped"` (FLARE's gear is always engaged, no physical selector — `[ASSUMED]` per RESEARCH.md A3, document as a code comment); `'last_tool': self.tool`; `'next_tool': self.tool` (1:1 gate==tool, synchronous toolchange — `[ASSUMED]` per RESEARCH.md, comment it); `'slicer_tool_map': {}`; `'endless_spool_enabled': self.reload_mode == 1` (`[ASSUMED]` mapping per RESEARCH.md A4, comment it); `'endless_spool_groups': list(range(self.num_gates))`; `'toolchange_purge_volume': 0.0`; `'encoder': None` (literal `None`, not omitted — Pitfall 2); `'clog_detection_enabled': False` (new correctly-spelled key; keep the existing `'clogs_enabled': False` line unchanged immediately above it as a backward-compatible alias for any custom macros — do not delete it).

    Extend `scripts/test_flare_mmu_status.py`'s existing reconcile-fields-dict `check()` block's expected-keys assertion (or add one immediately after it) to assert every one of the 18 keys above, plus `flowguard`/`sync_drive`/`is_paused`/`reason_for_pause`/`sync_feedback_flow_rate`/`baseline_sps` from Tasks 1-2, is present in a freshly-`SET_MMU`'d mock's `get_status(0)` with the expected type (bool/str/float/int/list/dict/None as appropriate) — this is the Plan-01 partial coverage; the full 93-key "before ready" schema assertion (`REQ-klipper-status-parity-schema-test`) is Plan 02's job, not this task's.

    Run `python3 -m py_compile klipper/*.py` and confirm it exits 0 before considering this task done.
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/*.py klipper/*.py && python3 -m unittest scripts.test_flare_mmu_status -v && python3 -m unittest discover -s scripts -p 'test_*.py'</automated>
    <fails_when>non-zero exit code, or "FAILED" appears in either unittest run's output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "'clog_detection_enabled': False" klipper/mmu.py` is `>= 1`, and `grep -c "'clogs_enabled': False" klipper/mmu.py` is still `>= 1` (alias retained, not deleted).
    - `grep -c "'encoder': None" klipper/mmu.py` is `>= 1` (literal key present, per Pitfall 2 — not omitted).
    - `python3 -m unittest discover -s scripts -p 'test_*.py'` reports `OK` on the last line (full suite, no regression from earlier phases).
  </acceptance_criteria>
  <done>All 18 static/derived keys plus the renamed `clog_detection_enabled` (with `clogs_enabled` alias retained) are present in `get_status()` with correct types; the phase's FLARE-sourced portion of Success Criterion 2 (missing keys) is complete; full regression suite is green.</done>
  <reversibility rating="reversible">Static literals and arithmetic over existing attributes only; no behavior change to any existing key. Revert via git.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker gcode/script → `cmd_SET_MMU` | The daemon pushes `SET_MMU k=v ...` via Moonraker's `/printer/gcode/script`; any client with Moonraker write access (not just the FLARE daemon) can call `SET_MMU`/`MMU_*` directly — an existing, out-of-phase trust boundary, not introduced by this plan. |
| Serial `ST:` line → `parse_status_line` | The firmware's status dump is trusted hardware telemetry, but a corrupted/partial serial read must not crash the reader thread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `cmd_SET_MMU` new `FLOWGUARD_*`/`SYNC_DRIVE`/`IS_PAUSED`/`REASON_FOR_PAUSE`/`BASELINE_SPS` params | low | accept | Matches the existing codebase-wide `SET_MMU` trust model: all 30+ pre-existing fields accept any value a Moonraker gcode caller supplies with no additional bounds validation beyond Klipper's own `gcmd.get_int`/`get_float` type coercion. New fields introduce no new trust boundary; `SET_MMU` is not reachable without Moonraker write access, which is out of this phase's scope. |
| T-14-02 | Denial of Service | `parse_status_line` new `TT:`/`CT:` branches | medium | mitigate | The two new `elif` branches sit inside the pre-existing `try: ... except ValueError: pass` block (`scripts/flare_daemon.py:511-598`) so a malformed dwell value (non-numeric, truncated line) is silently skipped — the field simply keeps its last-known `status_cache` value — rather than crashing the serial reader thread. Task 1's acceptance criteria explicitly checks the new branches sit inside this guarded chain. |
| T-14-03 | Information Disclosure | `flowguard.reason` / `reason_for_pause` surfaced to any Moonraker/Fluidd client | low | accept | Both derive from already-public daemon state (`mmu_stats["last_error"]`, dwell timers) already exposed via `GET /status` and the existing `last_error` status key; no new sensitive data is surfaced. |

</threat_model>

<verification>
Run `python3 scripts/validate_regression.py` after Task 3's commit — build/sim steps are unaffected by this phase (host-Python-only diff) but must stay green per `AGENTS.md` rule 1/9. Confirm `python3 -m unittest discover -s scripts -p 'test_*.py'` passes with no regressions in `test_flare_daemon_stats.py`, `test_flare_daemon_syncer.py`, or `test_flare_daemon_security.py` (none of which this plan's diff should affect, but they exercise adjacent `flare_daemon.py` code paths).
</verification>

<success_criteria>
- `printer.mmu.flowguard` exists with the exact 7-key shape and the 0/inactive-when-sync-off invariant, derived purely from `TT:`/`CT:` firmware fields already on the wire.
- Every FLARE-sourceable key from the RESEARCH.md diff table (23 keys total across the three tasks: flowguard, sync_drive, is_paused, reason_for_pause, sync_feedback_flow_rate, plus the 18 static/derived keys in Task 3) is present in `get_status()` with the correct type.
- `clog_detection_enabled` exists (rename bug fixed) with `clogs_enabled` retained as a backward-compatible alias.
- Six phase REQ IDs registered in `.planning/REQUIREMENTS.md`; `.planning/ROADMAP.md` Phase 14 no longer says `TBD`.
- `python3 -m unittest discover -s scripts -p 'test_*.py'` and `python3 -m py_compile scripts/*.py klipper/*.py` both green.
</success_criteria>

<output>
Create `.planning/phases/14-klipper-mmu-status-parity/14-01-SUMMARY.md` when done.
</output>