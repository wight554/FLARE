# Plan 02-03: Injectable Retract Guard & Validation

**Goal**: Provide non-invasive wrappers for foreign slicer/Klipper print-end macros and validate end-to-end.

## Tasks
- [x] Add `_FLARE_RETRACT_GUARD_BEGIN` (arms `BL:T` blocking with configurable `TIMEOUT=`) to `klipper/flare_mmu.cfg`
- [x] Add `_FLARE_RETRACT_GUARD_END` (closing `BS` + sync toolhead) to `klipper/flare_mmu.cfg`
- [x] Document wrapper pattern in `KLIPPER.md` for `rename_existing: END_PRINT`
- [x] Add reserve budget warning in macro if commanded retract exceeds buffer volume
- [x] Add host sim test scenarios in `scripts/test_sync_sim.py` covering fast prime prediction and catch escalation
- [x] Validate regression build: `python3 scripts/validate_regression.py` and `ninja -C build_local`
