# Plan 02-03: Injectable Retract Guard & Validation

**Goal**: Provide non-invasive wrappers for foreign slicer/Klipper print-end macros and validate end-to-end.

## Tasks
- [ ] Add `_FLARE_RETRACT_GUARD_BEGIN` (arms `BL:T` blocking with configurable `TIMEOUT=`) to `klipper/flare_mmu.cfg`
- [ ] Add `_FLARE_RETRACT_GUARD_END` (closing `BS` + sync toolhead) to `klipper/flare_mmu.cfg`
- [ ] Document wrapper pattern in `KLIPPER.md` for `rename_existing: END_PRINT`
- [ ] Add reserve budget warning in macro if commanded retract exceeds buffer volume
- [ ] Add host sim test scenarios in `scripts/test_sync_sim.py` covering fast prime prediction and catch escalation
- [ ] Validate regression build: `python3 scripts/validate_regression.py` and `ninja -C build_local`
