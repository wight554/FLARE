# Plan 02-01: Host Completion Wait & Macro Cleanup

**Goal**: Make host scripts and macros block on firmware buffer events instead of using blind `G4` dwell delays.

## Tasks
- [x] Update `scripts/flare_cmd.py` `COMPLETION_EVENTS`:
  - Add `'BL': (['EV:BL:LOCKED', 'EV:BL:PRIME_BOUND'], ['EV:BL:TIMEOUT'])`
  - Add `'BS': (['EV:BUF_STAB:DONE'], ['EV:BUF_STAB:TIMEOUT', 'EV:BUF_STAB:STAGNANT_TIMEOUT'])`
- [x] Verify `sync.c` emits `EV:BUF_STAB:DONE` on all exit paths (including already-at-goal no-op)
- [x] Update `klipper/flare_mmu.cfg`:
  - `_FLARE_BUFFER_STABILIZE`: default `SETTLE` to 0 (rely on blocking `BS`)
  - `_FLARE_BL_RETRACT`: drop `PAUSE` parameter and remove blind `G4` delays
  - `_FLARE_VARS`: set `mmu_follow_rate` to `5000.0` (= `GLOBAL_MAX_SPS`) to prevent silent clamping
