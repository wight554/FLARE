# Plan 02-02: Fast Type-P Prime & Servo Catch Escalation

**Goal**: Elevate Type-P prime speed and replace open-loop fixed follow with error-proportional rate-servo catch.

## Tasks
- [x] In `firmware/src/sync.c`:
  - Lift Type-P prime speed from `BUF_STAB_SPS` (10 mm/s) to `SYNC_MAX_SPS` (36.7 mm/s)
  - Add predictive rail stop (`g_buf_pos + BL_PRIME_PREDICT_LEAD_S * g_vel_norm_f`) to stop prime before slamming rail
  - Adopt full `BUF_MAX_TRAVEL_MM` prime cap for Type-P
- [x] In `firmware/src/sync.c` (`BL_FOLLOW` state):
  - Convert `BL_FOLLOW` into a rate-servo catch: seeded by commanded follow rate, ramping pull-in safe
  - Escalate step rate toward `GLOBAL_MAX_SPS` (83.3 mm/s) proportional to displacement away from armed rail
  - Emit `EV:BL:BREAK` on lock break
- [x] Add optional `timeout_ms` 4th argument to `BL` command in `firmware/src/protocol.c` (`BL:<state>:<follow_mm>:<follow_rate>:<timeout_ms>`)
