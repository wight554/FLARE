---
phase: 16-tmc-tension-current-boost
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01-PLAN.md
files_modified:
  - firmware/src/protocol_status.c
  - scripts/test_status_line_budget.py
  - config.ini
  - config.ini.example
  - scripts/gen_config.py
  - firmware/include/settings_store.h
  - firmware/src/settings_store.c
  - firmware/src/protocol.c
  - scripts/test_settings_parity.py
  - scripts/flare_daemon.py
  - scripts/flare_cmd.py
  - tests/host/sim_scenario.c
  - MANUAL.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - R4
  - R5
  - R7

estimate:
  tokens: 75000
  raw_tokens: 55000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Extended ST: status line reports TB:0 or TB:1 within the strict STATUS_LINE_MAX character budget with mathematical headroom proof"
    - "Tension boost configuration parameters (SYNC_TENSION_BOOST_IRUN, SYNC_TENSION_BOOST_ON, SYNC_TENSION_BOOST_OFF) round-trip cleanly across config.ini, flash TLV storage, SET/GET, and flare_cmd.py --dump"
    - "SET commands reject invalid threshold ordering (BOOST_ON >= BOOST_OFF) with ER:INVALID_PARAM and clamp boost current to 1200 mA max"
    - "flare_daemon.py parses TB token into status cache boolean and flare_cmd.py displays it in status monitoring"
    - "Integration plant simulation scenario sem_psf_tension_boost demonstrates dynamic boost under spool drag and clean escalation to Phase 13 fault trip under persistent stall"
    - "MANUAL.md documents all three runtime configuration knobs, status line token TB, and edge transition events TMC:BOOST / TMC:NORMAL"
    - ".planning/REQUIREMENTS.md contains full definitions and traceability entries for R1 through R7 and ROADMAP.md lists them under Phase 16"
  artifacts:
    - firmware/src/protocol_status.c (appends ,TB:%c to extended status tail)
    - scripts/test_status_line_budget.py (proves 759/760 character budget headroom with TB:%c)
    - config.ini & config.ini.example (boost knobs with safety documentation)
    - scripts/gen_config.py (code generation for boost constants)
    - firmware/include/settings_store.h (SETTINGS_VERSION 65u and TLV tags 67, 68, 69)
    - firmware/src/settings_store.c (defaults, serialization, deserialization, and bounds clamping)
    - firmware/src/protocol.c (GET/SET parameter parsing and strict ordering validation)
    - scripts/test_settings_parity.py (passes TLV and dump rebuild parity)
    - scripts/flare_daemon.py & scripts/flare_cmd.py (status token ingestion and --dump emission)
    - tests/host/sim_scenario.c (sem_psf_tension_boost integration scenario)
    - MANUAL.md (parameter reference, diagnostic status field, and events documentation)
    - .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md (phase completion and requirement registration)
  key_links:
    - "protocol_status.c -> cmd_handle_status_dump -> snprintf ,TB:%c -> test_status_line_budget.py"
    - "gen_config.py -> tune.h -> main.c / settings_store.c -> test_settings_parity.py"
    - "protocol.c -> cmd_apply_set_param -> validates BOOST_ON < BOOST_OFF and clamp_i(iv, 0, 1200)"
    - "sim_scenario.c -> sem_psf_tension_boost -> flare_sim -> validates end-to-end plant behavior and Phase 13 fault trip"
    - "MANUAL.md -> documents SYNC_TENSION_BOOST_* knobs, TB status token, and TMC:BOOST/NORMAL events per Success Criterion 3"
  manual: []
---

<objective>
Wire tension boost telemetry token (`TB:%c`) into the serial status dump, implement configuration parity across `config.ini`, TLV flash storage, SET/GET protocol, and CLI tools, add integration plant simulation scenario `sem_psf_tension_boost`, document parameters and events in `MANUAL.md`, and register full requirement definitions in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.

Purpose: Fulfill Requirements R4, R5, and R7 of Phase 16, completing user and host visibility, configuration knobs, end-to-end integration plant verification, and documentation synchronization per ROADMAP.md Success Criterion 3 and AGENTS.md Rules 6 & 8.
Output: Telemetry reporting in `firmware/src/protocol_status.c`, configuration parity across firmware and host tools, plant simulation scenario in `tests/host/sim_scenario.c`, updated user documentation in `MANUAL.md`, and registered requirement records in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-tmc-tension-current-boost/16-CONTEXT.md
@.planning/phases/16-tmc-tension-current-boost/16-RESEARCH.md
@.planning/phases/16-tmc-tension-current-boost/16-PATTERNS.md
@.planning/phases/16-tmc-tension-current-boost/16-SPEC.md
@.planning/phases/16-tmc-tension-current-boost/16-VALIDATION.md
@firmware/src/protocol_status.c
@scripts/test_status_line_budget.py
@firmware/include/settings_store.h
@firmware/src/settings_store.c
@firmware/src/protocol.c
@scripts/gen_config.py
@tests/host/sim_scenario.c
@MANUAL.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate protocol status telemetry token TB:%c and verify line budget</name>
  <files>firmware/src/protocol_status.c, scripts/test_status_line_budget.py</files>
  <read_first>
    firmware/src/protocol_status.c:100-130 (status dump format string tail, including PR:%c);
    scripts/test_status_line_budget.py:1-180 (STATUS_LINE_MAX mathematical headroom calculation);
    firmware/include/sync.h:20-35 (sync_is_tension_boost_active prototype).
  </read_first>
  <action>
    In `firmware/src/protocol_status.c`:
    1. In `cmd_handle_status_dump(char *out, size_t out_len)`:
       - Query active lane tension boost state: `char tb_char = sync_is_tension_boost_active(g_active_lane) ? '1' : '0';` (Decision D-06, Requirement R5).
       - Append `,TB:%c` to the extended status tail format string immediately following `,PR:%c`.
       - Pass `tb_char` as the argument to `snprintf`.

    In `scripts/test_status_line_budget.py`:
    1. Verify that `test_status_line_fits_budget_with_headroom` accounts for `,TB:%c`.
    2. Confirm that the worst-case line length remains strictly below `STATUS_LINE_MAX` (759/760 characters, with 1 character of headroom remaining).
  </action>
  <verify>
    <automated>python3 scripts/test_status_line_budget.py</automated>
    <fails_when>test_status_line_budget fails or headroom is <= 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "TB:%c" firmware/src/protocol_status.c` is `>= 1`.
    - `python3 scripts/test_status_line_budget.py` exits 0 and reports positive headroom (`ST: line worst-case 759/760 chars (1 chars headroom)`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement configuration parity across config.ini, code generation, TLV persistence, and SET/GET</name>
  <files>config.ini, config.ini.example, scripts/gen_config.py, firmware/include/settings_store.h, firmware/src/settings_store.c, firmware/src/protocol.c, scripts/test_settings_parity.py</files>
  <read_first>
    config.ini.example:205-230 (Buffer Sync configuration section);
    scripts/gen_config.py:110-130 (DEFAULTS dictionary mapping);
    scripts/gen_config.py:530-560 (header macro emission);
    firmware/include/settings_store.h:8-10, 80-90 (SETTINGS_VERSION and settings_tag_t enum);
    firmware/src/settings_store.c:480-515 (settings_save serialization);
    firmware/src/settings_store.c:630-660 (settings_load_tlv_tag deserialization);
    firmware/src/settings_store.c:890-935 (settings_apply_clamps bounds enforcement);
    firmware/src/protocol.c:460-475 (cmd_get_param GET handlers);
    firmware/src/protocol.c:990-1025, 1080-1120 (cmd_apply_set_param and cmd_set_buffer_params SET handlers).
  </read_first>
  <action>
    In `config.ini.example` and `config.ini`:
    1. In the `[buffer]` or `[sync]` configuration section, add:
       - `# sync_tension_boost_irun = 0  ; mA gear motor boost during deep tension (0 = disabled, max 1200)`
       - `# sync_tension_boost_on = -0.50  ; Type-P buffer position to engage boost (must be < boost_off)`
       - `# sync_tension_boost_off = -0.30 ; Type-P buffer position to release boost via hysteresis`
       - Include explicit comments documenting safety ceiling (1200 mA) and disabled-by-default policy (Decision D-09, Requirement R7).

    In `scripts/gen_config.py`:
    1. In `DEFAULTS`:
       - Add `"sync_tension_boost_irun": "0"`.
       - Add `"sync_tension_boost_on": "-0.50"`.
       - Add `"sync_tension_boost_off": "-0.30"`.
    2. In header code emission:
       - Emit `#define CONF_SYNC_TENSION_BOOST_ON {get_float('sync_tension_boost_on')}f`.
       - Emit `#define CONF_SYNC_TENSION_BOOST_OFF {get_float('sync_tension_boost_off')}f`.
       - Emit `#define CONF_L1_SYNC_TENSION_BOOST_IRUN {get_int('sync_tension_boost_irun', lane=1)}`.
       - Emit `#define CONF_L2_SYNC_TENSION_BOOST_IRUN {get_int('sync_tension_boost_irun', lane=2)}`.

    In `firmware/include/settings_store.h`:
    1. Update settings version:
       - Define `#define SETTINGS_VERSION_V64 64u`.
       - Bump `#define SETTINGS_VERSION 65u`.
    2. In `settings_tag_t` enum:
       - Add `TAG_SYNC_TENSION_BOOST_IRUN = 67,`.
       - Add `TAG_SYNC_TENSION_BOOST_ON = 68,`.
       - Add `TAG_SYNC_TENSION_BOOST_OFF = 69,`.

    In `firmware/src/settings_store.c`:
    1. In `settings_defaults()`:
       - Seed `g_sync_tension_boost_irun[0] = clamp_i(CONF_L1_SYNC_TENSION_BOOST_IRUN, 0, 1200);`.
       - Seed `g_sync_tension_boost_irun[1] = clamp_i(CONF_L2_SYNC_TENSION_BOOST_IRUN, 0, 1200);`.
       - Seed `g_sync_tension_boost_on = clamp_f(CONF_SYNC_TENSION_BOOST_ON, -1.0f, -0.05f);`.
       - Seed `g_sync_tension_boost_off = clamp_f(CONF_SYNC_TENSION_BOOST_OFF, -0.95f, 0.0f);`.
    2. In `settings_save()`:
       - Emit `tlv_emit(&w, TAG_SYNC_TENSION_BOOST_IRUN, sizeof(g_sync_tension_boost_irun), g_sync_tension_boost_irun);`.
       - Emit `tlv_emit_f32(&w, TAG_SYNC_TENSION_BOOST_ON, g_sync_tension_boost_on);`.
       - Emit `tlv_emit_f32(&w, TAG_SYNC_TENSION_BOOST_OFF, g_sync_tension_boost_off);`.
    3. In `settings_load_tlv_tag()`:
       - Add `case TAG_SYNC_TENSION_BOOST_IRUN:` check `len == sizeof(g_sync_tension_boost_irun)` and `memcpy`.
       - Add `case TAG_SYNC_TENSION_BOOST_ON:` check `len == sizeof(float)` and `memcpy`.
       - Add `case TAG_SYNC_TENSION_BOOST_OFF:` check `len == sizeof(float)` and `memcpy`.
    4. In `settings_apply_clamps()`:
       - Clamp `g_sync_tension_boost_irun[i] = clamp_i(g_sync_tension_boost_irun[i], 0, 1200);` for both lanes.
       - Clamp `g_sync_tension_boost_on = clamp_f(g_sync_tension_boost_on, -1.0f, -0.05f);`.
       - Clamp `g_sync_tension_boost_off = clamp_f(g_sync_tension_boost_off, -0.95f, 0.0f);`.
       - Enforce ordering invariant: `if (g_sync_tension_boost_on >= g_sync_tension_boost_off) { g_sync_tension_boost_on = -0.50f; g_sync_tension_boost_off = -0.30f; }`.

    In `firmware/src/protocol.c`:
    1. In `cmd_get_param()`:
       - Handle `SYNC_TENSION_BOOST_IRUN`: format `SYNC_TENSION_BOOST_IRUN:%d` using `g_sync_tension_boost_irun[idx]`.
       - Handle `SYNC_TENSION_BOOST_ON`: format `SYNC_TENSION_BOOST_ON:%.2f` using `g_sync_tension_boost_on`.
       - Handle `SYNC_TENSION_BOOST_OFF`: format `SYNC_TENSION_BOOST_OFF:%.2f` using `g_sync_tension_boost_off`.
    2. In `cmd_apply_set_param()` / `cmd_set_buffer_params()`:
       - Handle `SYNC_TENSION_BOOST_IRUN`:
         - Validate integer: reject `iv < 0` with `ER:INVALID_PARAM`.
         - Clamp value: `int clamped = clamp_i(iv, 0, 1200);`.
         - Assign to lane: `SET_LANE({ g_sync_tension_boost_irun[idx] = clamped; });`.
       - Handle `SYNC_TENSION_BOOST_ON`:
         - Validate strictly more negative than off threshold: `if (fv >= g_sync_tension_boost_off) { cmd_reply("ER", "INVALID_PARAM"); return CMD_SET_REPLIED; }`.
         - Assign clamped value: `g_sync_tension_boost_on = clamp_f(fv, -1.0f, -0.05f);`.
       - Handle `SYNC_TENSION_BOOST_OFF`:
         - Validate strictly less negative than on threshold: `if (fv <= g_sync_tension_boost_on) { cmd_reply("ER", "INVALID_PARAM"); return CMD_SET_REPLIED; }`.
         - Assign clamped value: `g_sync_tension_boost_off = clamp_f(fv, -0.95f, 0.0f);`.
  </action>
  <verify>
    <automated>python3 scripts/test_settings_parity.py &amp;&amp; python3 scripts/gen_config.py</automated>
    <fails_when>test_settings_parity fails or gen_config throws errors</fails_when>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/test_settings_parity.py` passes with 0 errors.
    - `grep -c "SETTINGS_VERSION 65u" firmware/include/settings_store.h` is 1.
    - `grep -c "TAG_SYNC_TENSION_BOOST_IRUN" firmware/include/settings_store.h` is 1.
    - `grep -c "SYNC_TENSION_BOOST_ON" firmware/src/protocol.c` is `>= 2` (GET and SET handlers).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement host daemon/CLI telemetry, plant simulation scenario, documentation sync in MANUAL.md, and requirement registration</name>
  <files>scripts/flare_daemon.py, scripts/flare_cmd.py, tests/host/sim_scenario.c, MANUAL.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    scripts/flare_daemon.py:880-950 (status line key-value parsing);
    scripts/flare_cmd.py:110-130 (DUMP_PARAMS table);
    scripts/flare_cmd.py:460-485 (poll_status line formatting);
    tests/host/sim_scenario.c:690-735 (analog plant scenarios testing Type-P buffer dynamics);
    MANUAL.md:213-245 (Safety & Timeouts parameter table);
    MANUAL.md:326-350 (Diagnostic Status Fields table);
    MANUAL.md:359-385 (Events table);
    .planning/phases/16-tmc-tension-current-boost/16-SPEC.md:24-60 (R1 through R7 locked requirements);
    .planning/ROADMAP.md:270-285 (Phase 16 goals, requirements line, and milestone criteria);
    .planning/REQUIREMENTS.md:670-715, 2120-2178 (requirement definition format and traceability matrix).
  </read_first>
  <action>
    In `scripts/flare_daemon.py`:
    1. In `parse_status_line(line)`:
       - Add branch: `elif key == "TB": new_data["tension_boost"] = bool(int(val))` inside the guarded parsing chain.

    In `scripts/flare_cmd.py`:
    1. In `DUMP_PARAMS`:
       - Add `("SYNC_TENSION_BOOST_IRUN", "sync_tension_boost_irun", True)`.
       - Add `("SYNC_TENSION_BOOST_ON", "sync_tension_boost_on", False)`.
       - Add `("SYNC_TENSION_BOOST_OFF", "sync_tension_boost_off", False)`.
    2. In `poll_status()`:
       - Append `TB:1` if `status.get("tension_boost")` else `TB:0` to status string display.

    In `tests/host/sim_scenario.c`:
    1. Define plant simulation scenario `sem_psf_tension_boost`:
       - Configure demand step and spool friction drag that pulls Type-P buffer position to <= -0.50.
       - Assert that tension boost activates (`TMC:BOOST` emitted, `TB:1`), gear torque overcomes friction, and as buffer relaxes to >= -0.30, boost releases (`TMC:NORMAL` emitted, `TB:0`).
       - Validate persistent tangle behavior: under sustained unrecoverable drag, Phase 13 distance (`SYNC_TENSION_STOP_MM`) or dwell (`SYNC_TENSION_DWELL_STOP_MS`) fault trip engages `SYNC_FAULT_HOLD`, halts motion, and unconditionally restores baseline current (Requirement R4).

    In `MANUAL.md`:
    1. In `### Safety & Timeouts` parameter table, add:
       - `SYNC_TENSION_BOOST_IRUN` | `sync_tension_boost_irun` | Extra gear motor run current in mA applied during active sync Type-P tension boost (Happy-Hare tangle prevention). 0 = disabled (default). Hardware clamped at 1200 mA max. | 0
       - `SYNC_TENSION_BOOST_ON` | `sync_tension_boost_on` | Type-P buffer position threshold to activate tension current boost (negative travel; must be strictly < `SYNC_TENSION_BOOST_OFF`). | -0.50
       - `SYNC_TENSION_BOOST_OFF` | `sync_tension_boost_off` | Type-P buffer position threshold to release tension current boost back to baseline run current via hysteresis (must be strictly > `SYNC_TENSION_BOOST_ON`). | -0.30
    2. In `### Diagnostic Status Fields` table, add:
       - `TB` | 0/1 | Active lane Type-P tension boost state: `1` = motor run current boosted to `SYNC_TENSION_BOOST_IRUN`, `0` = running at baseline current.
    3. In `## Events (`EV:`)` table, add:
       - `TMC:BOOST` | `lane` | Tension boost engaged on specified lane: motor run current raised to `SYNC_TENSION_BOOST_IRUN`.
       - `TMC:NORMAL` | `lane` | Tension boost released / disengaged on specified lane: motor run current restored to baseline `g_tmc_run_current_ma`.

    In `.planning/REQUIREMENTS.md`:
    1. Register full requirement definitions for R1 through R7 from `16-SPEC.md`:
       - `### REQ-tmc-tension-boost-activation` (R1): In `SYNC_ACTIVE` with Type-P buffer, when buffer position reaches `g_buf_pos <= SYNC_TENSION_BOOST_ON`, active lane TMC run current raises to `SYNC_TENSION_BOOST_IRUN` (clamped <= 1200 mA).
       - `### REQ-tmc-tension-boost-hysteresis-release` (R2): When boost is active, when buffer position relaxes to `g_buf_pos >= SYNC_TENSION_BOOST_OFF`, active lane TMC run current restores to baseline `g_tmc_run_current_ma[lane]`.
       - `### REQ-tmc-tension-boost-unconditional-reset` (R3): Any exit from `SYNC_ACTIVE` (`STOP`, `PA`, `sync_disable()`, fault trip, toolchange `TC:`, lane switch) immediately resets boost flag and restores base run current.
       - `### REQ-tmc-tension-boost-persistent-tangle-escalation` (R4): Unrelieved tension defers to Phase 13 distance (`SYNC_TENSION_STOP_MM`) and dwell (`SYNC_TENSION_DWELL_STOP_MS`) fault trips, halting motion cleanly via `stop_all()` and restoring base current without indefinite boost lockup.
       - `### REQ-tmc-tension-boost-telemetry-visibility` (R5): Status `ST:` line reports `TB:0` or `TB:1` within character budget; transitions emit `EV:TMC:BOOST:<lane>` and `EV:TMC:NORMAL:<lane>`.
       - `### REQ-tmc-tension-boost-shadow-register-integrity` (R6): TMC heartbeat recovery and register shadow state stay synchronized with active boost current so brownout recovery re-applies the true active current without clobbering boost.
       - `### REQ-tmc-tension-boost-config-parity` (R7): Parameters `SYNC_TENSION_BOOST_IRUN`, `SYNC_TENSION_BOOST_ON`, `SYNC_TENSION_BOOST_OFF` supported across `config.ini`, flash TLV storage, `SET:` / `GET:`, and `flare_cmd.py --dump` with strict ordering validation (`BOOST_ON < BOOST_OFF`).
    2. Add traceability mapping rows to the Active Requirements matrix:
       - `| REQ-tmc-tension-boost-activation | Phase 16 | Complete |`
       - `| REQ-tmc-tension-boost-hysteresis-release | Phase 16 | Complete |`
       - `| REQ-tmc-tension-boost-unconditional-reset | Phase 16 | Complete |`
       - `| REQ-tmc-tension-boost-persistent-tangle-escalation | Phase 16 | Complete |`
       - `| REQ-tmc-tension-boost-telemetry-visibility | Phase 16 | Complete |`
       - `| REQ-tmc-tension-boost-shadow-register-integrity | Phase 16 | Complete |`
       - `| REQ-tmc-tension-boost-config-parity | Phase 16 | Complete |`

    In `.planning/ROADMAP.md`:
    1. Update Phase 16 requirements line from `**Requirements**: TBD (derive from research §1.5)` to:
       `**Requirements**: R1, R2, R3, R4, R5, R6, R7 (REQ-tmc-tension-boost-activation, REQ-tmc-tension-boost-hysteresis-release, REQ-tmc-tension-boost-unconditional-reset, REQ-tmc-tension-boost-persistent-tangle-escalation, REQ-tmc-tension-boost-telemetry-visibility, REQ-tmc-tension-boost-shadow-register-integrity, REQ-tmc-tension-boost-config-parity)`
    2. Update Phase 16 Plans section:
       - Mark `[x] 16-01-PLAN.md` and `[x] 16-02-PLAN.md`.
    3. Update Progress table for Phase 16 marking plans complete.

    In `.planning/STATE.md`:
    1. Update current phase status, recording successful execution of Phase 16 Wave 2 and completion of milestone objectives.
  </action>
  <verify>
    <automated>python3 -m py_compile scripts/flare_daemon.py scripts/flare_cmd.py &amp;&amp; python3 scripts/test_status_line_budget.py &amp;&amp; python3 scripts/test_settings_parity.py &amp;&amp; python3 scripts/validate_regression.py</automated>
    <fails_when>py_compile failure, test failure, or validate_regression non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "elif key == \"TB\":" scripts/flare_daemon.py` is `>= 1`.
    - `grep -c "SYNC_TENSION_BOOST_IRUN" scripts/flare_cmd.py` is `>= 1`.
    - `grep -c "sem_psf_tension_boost" tests/host/sim_scenario.c` is `>= 1`.
    - `grep -c "SYNC_TENSION_BOOST_IRUN" MANUAL.md` is `>= 1`.
    - `grep -c "TB" MANUAL.md` is `>= 1`.
    - `grep -c "TMC:BOOST" MANUAL.md` is `>= 1`.
    - `grep -c "REQ-tmc-tension-boost-activation" .planning/REQUIREMENTS.md` is `>= 1`.
    - `grep -c "R1, R2, R3, R4, R5, R6, R7" .planning/ROADMAP.md` is `>= 1`.
    - `python3 scripts/validate_regression.py` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host serial interface (`flare_daemon.py`, `flare_cmd.py`) | Boundary between host daemons and firmware serial protocol. Corrupted status lines or malformed inputs must not crash services. |
| User configuration & `SET:` commands | Operator input boundary. Malformed parameters or inverted thresholds must not cause driver oscillation or excessive current. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Denial of Service | `firmware/src/protocol_status.c` (`cmd_handle_status_dump`) | medium | mitigate | Status line buffer overrun causing silent truncation of downstream status fields. Mitigated by strictly budgeted single-char token `,TB:%c` and validated by `scripts/test_status_line_budget.py` proving worst-case is 759/760 chars (1 char margin). |
| T-16-06 | Tampering / Denial of Service | `firmware/src/protocol.c` (`cmd_apply_set_param`) | medium | mitigate | Inverted or degenerate hysteresis band (`BOOST_ON >= BOOST_OFF`) causing UART bus flooding or infinite oscillation. Mitigated by strict order validation in `protocol.c` returning `ER:INVALID_PARAM` if `BOOST_ON >= BOOST_OFF`. |
| T-16-07 | Elevation of Privilege / Safety | `firmware/src/protocol.c` & `settings_store.c` | high | mitigate | User injecting dangerous motor current (> 1200 mA) via `SET:SYNC_TENSION_BOOST_IRUN`. Mitigated by clamping `iv` via `clamp_i(iv, 0, 1200)` in `protocol.c` and in `settings_apply_clamps()`. |
</threat_model>

<verification>
Run full regression suite:
`python3 scripts/validate_regression.py && python3 scripts/test_status_line_budget.py && python3 scripts/test_settings_parity.py`
Confirm clean compilation, zero regressions, and full parameter parity.
</verification>

<success_criteria>
- Telemetry token `TB:%c` reports `1` when active lane is boosted and `0` when normal, staying strictly within `STATUS_LINE_MAX`.
- Configuration knobs `SYNC_TENSION_BOOST_IRUN`, `SYNC_TENSION_BOOST_ON`, and `SYNC_TENSION_BOOST_OFF` fully persist in flash TLV storage and round-trip through `flare_cmd.py --dump`.
- `SET:` parameter validation rejects `BOOST_ON >= BOOST_OFF` and enforces 1200 mA maximum current ceiling.
- Host daemon and CLI tools expose live boost telemetry.
- Simulation scenario `sem_psf_tension_boost` validates physical plant behavior under step drag and persistent tangle fault escalation.
- MANUAL.md reflects the new knobs, telemetry token, and edge events per ROADMAP.md Success Criterion 3 and AGENTS.md.
- .planning/REQUIREMENTS.md and ROADMAP.md contain the full registered definitions and traceability mapping for R1..R7.
- Full regression validation passes cleanly.
</success_criteria>
