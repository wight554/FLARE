---
phase: 13-type-p-sync-relief-fault-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen_config.py
  - config.ini.example
  - firmware/src/main.c
  - firmware/include/controller_shared.h
  - firmware/include/settings_store.h
  - firmware/src/settings_store.c
  - firmware/src/protocol.c
  - firmware/include/sync_internal.h
  - firmware/src/sync.c
  - tests/host/sim_scenario.c
  - scripts/test_sync_sim.py
  - scripts/flare_cmd.py
  - MANUAL.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-type-p-sync-relief-bounded-refill
  - REQ-type-p-sync-relief-rail-relative-triggers
  - REQ-type-p-sync-relief-sim-coverage

estimate:
  tokens: 112000
  raw_tokens: 112000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "In type-P mode, no code path commands the raw clamped max step rate while the plant reports the buffer saturated at the tension rail — asserted unconditionally in flare_sim at type_p_rail_scale 1.0, 0.7 and 0.5 (D-01, D-04, the single invariant named in 13-CONTEXT.md <specifics>)"
    - "Relief feed rises through the existing distance-EMA/slew path with a doubled slew cap, not as a one-tick step: the per-tick change in commanded feed while relieving stays bounded by the doubled slew allowance instead of jumping the full distance to the soft-wall target in one tick (D-02, D-03)"
    - "Bounded relief is actually prompt on a 16 mm buffer: while relieving, the 25 mm target EMA is shortened by SYNC_RELIEF_FILTER_DIV so commanded feed reaches at least 90% of the relief bound within one buffer-travel of extruded filament after RELIEF_ON — bounded must not mean sluggish (REVIEW-01)"
    - "Neither SYNC_PSF_RELIEF_MULT nor any other new float knob can enter the control loop out of range from a corrupt or foreign flash sector: settings_apply_clamps() re-clamps it after every TLV load, the same way g_relay_catchup_frac is re-clamped today (REVIEW-05)"
    - "A single uncalibrated deep deflection spike cannot depress the relief threshold for the rest of a long print: g_sync_tension_extreme re-seeds to the current reading once the buffer is observed at or above extreme + 2x BL_BREAK_DELTA_NORM, still rail-relative and still clock-free (REVIEW-07)"
    - "EV:SYNC:RELIEF_ON is emitted on entry to the relief zone and EV:SYNC:RELIEF_OFF on exit, once per edge and never per tick, at every tested rail scale including 0.5 — the exact 51bdca8 failure class the tripwire exists to catch (D-06, D-25)"
    - "Relief-zone entry is decided against an observed per-sync-window tension extreme plus a margin once an extreme exists, and against the debounced BUF_TENSION state before one does; sync.c contains no absolute normalized-position literal for this decision (D-22, D-23)"
    - "SYNC_PSF_RELIEF_MULT survives a settings save/load round trip and is readable via GET: in a release build (no dev-tuning gate), per D-28 and the TAG_SYNC_KP_SPS template rather than the FLARE_DEV_TUNING-gated ms-trip template"
  artifacts:
    - firmware/src/sync.c (sync_type_p_relief_bound_sps() helper, sync_type_p_in_relief_zone() helper, g_sync_tension_extreme tracking + relaxation + reset wiring, rewritten urgent-refill branch, relief cap on the tension ramp path, RELIEF_ON/RELIEF_OFF edge events, sync_apply_type_p_smoothing() widened with slew and filter-length parameters)
    - firmware/include/sync_internal.h (SOFT_WALL_MARGIN_NORM, SYNC_RELIEF_SLEW_MULT, SYNC_RELIEF_FILTER_DIV, SYNC_EXTREME_RELAX_MULT)
    - firmware/src/main.c + firmware/include/controller_shared.h (g_sync_psf_relief_mult definition + extern)
    - firmware/include/settings_store.h + firmware/src/settings_store.c (new TLV tag, default, emit, load case, post-load re-clamp in settings_apply_clamps())
    - firmware/src/protocol.c (release-build SET:/GET: SYNC_PSF_RELIEF_MULT with clamp)
    - scripts/gen_config.py + config.ini.example (sync_psf_relief_mult DEFAULTS entry + CONF_SYNC_PSF_RELIEF_MULT emission + documented example line)
    - tests/host/sim_scenario.c (sem_psf_relief_bound, sem_psf_relief_bound_shallow, sem_psf_relief_shallow_rail)
    - scripts/test_sync_sim.py (_conf_int tune.h reader, bound-invariant tests, updated expected trace for the former snap test)
    - .planning/REQUIREMENTS.md + .planning/ROADMAP.md (six REQ-type-p-sync-relief-* IDs registered, Phase 13 Requirements line no longer TBD)
  key_links:
    - "config.ini(.example) sync_psf_relief_mult -> gen_config.py DEFAULTS -> CONF_SYNC_PSF_RELIEF_MULT in tune.h -> g_sync_psf_relief_mult in main.c -> settings_defaults_sync() -> tlv_emit_f32/settings_load_tlv_tag -> protocol.c SET/GET -> sync_type_p_relief_bound_sps() -> sync_apply_type_p_smoothing() -> g_sync_current_sps"
    - "g_buf_pos deepening -> g_sync_tension_extreme -> (extreme + SOFT_WALL_MARGIN_NORM) relief-zone test -> RELIEF_ON edge -> g_psf_target_filt seeded at g_extruder_est_sps once -> bounded target through the shortened-filter, doubled-slew smoothing path"
    - "sync_rearm_active()/sync_disable()/AUTO_START reset g_sync_tension_extreme and its validity flag — if the reset is missed, a stale deep extreme from a previous window makes the relief zone unreachable and relief silently never fires (the same class of silent disable as 51bdca8)"
    - "settings_load_tlv() -> settings_apply_clamps() (settings_store.c:907 calls it immediately after the TLV sweep) — every new float knob must be re-clamped there, not only on the SET: path, or a corrupt flash sector injects an out-of-range value that no runtime command ever wrote (REVIEW-05)"
    - "SYNC_RELIEF_FILTER_DIV shortens filter_len_mm inside sync_apply_type_p_smoothing while relieving; it is the ONLY thing standing between a bounded relief target and a 25 mm-lagged ramp on a 16 mm buffer, and 13-03's probe reads the buffer motion this ramp produces — slow relief here becomes a false NO_CONSUMER there (REVIEW-01, and the 13-01/13-03 coupling the reviewer flagged)"
  manual:
    - "HW: rig A/B print confirming the hunting cadence actually drops (D-30/D-31) is plan 13-04's blocking checkpoint, not auto-checkable here"
---

<objective>
Replace the type-P urgent-refill snap with a bounded, rail-relative, slew-limited relief feed, and prove
in `flare_sim` — at three rail scales — that no path commands the raw clamped max step rate while the
buffer is pegged at tension. This is Phase 13 Success Criterion 1 plus the ROADMAP carried caveat, and it
is the tracer slice for the whole phase: it wires one behaviour through every layer Phase 13 touches
(config.ini -> gen_config -> tune.h -> main.c global -> TLV persistence -> `SET:`/`GET:` -> control loop ->
`EV:` events -> sim scenario -> docs), so plans 02 and 03 expand out from a proven spine rather than
discovering a layering problem after three commits.

Purpose: eliminate the "snap to compression rail -> RELIEF_PAUSE -> restart" hunting cycle captured in
`13-.../baseline-capture.md` (2.4-3.4 s cadence, dozens of cycles in 111 s), without repeating the
`51bdca8` mistake of gating a type-P behaviour on an absolute normalized-position literal.
Output: bounded relief live in `sync.c`, `SYNC_PSF_RELIEF_MULT` as a live + flash-persisted knob, three new
sim scenarios, the six phase REQ IDs registered.
</objective>

<execution_context>
@~/.claude-personal/gsd-core/workflows/execute-plan.md
@~/.claude-personal/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-type-p-sync-relief-fault-trip/13-CONTEXT.md
@.planning/phases/13-type-p-sync-relief-fault-trip/13-RESEARCH.md
@.planning/phases/13-type-p-sync-relief-fault-trip/baseline-capture.md
@AGENTS.md
@STYLE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — bounded relief feed end-to-end (config -> TLV -> SET/GET -> sync.c -> events -> sim)</name>
  <files>scripts/gen_config.py, config.ini.example, firmware/src/main.c, firmware/include/controller_shared.h, firmware/include/settings_store.h, firmware/src/settings_store.c, firmware/src/protocol.c, firmware/include/sync_internal.h, firmware/src/sync.c, tests/host/sim_scenario.c, scripts/test_sync_sim.py</files>
  <read_first>
    firmware/src/sync.c:2009-2025 (the urgent-refill branch being replaced: the type-P arm of the
      `if (fast_brake_active)` chain in `sync_tick_apply_rate`, whose guard compares `buf_pos_norm()`
      against the negated `CONF_PSF_SOFT_WALL_START` macro and whose body assigns the soft-wall target
      straight into the applied step rate — both of those go away in this task);
    firmware/src/sync.c:1857-1906 (`sync_apply_type_p_smoothing` — the distance-EMA + slew + wall-clock
      decay path the bounded target must be routed through; note the `max_step` floor of 1.0f and the
      decay guard comment explaining why UP must stay purely flow-keyed. Read step 1 closely:
      `filter_len_mm` comes from `g_sync_psf_filter_mm` (default `CONF_SYNC_PSF_FILTER_MM` = 25.0,
      scripts/gen_config.py:115) floored at the `PSF_FILTER_MIN_MM` 0.01f constant at sync.c:57, and
      `alpha = 1 - exp(-move/filter_len_mm)`. On the rig's 16 mm buffer a 25 mm EMA is longer than the
      entire buffer travel, so an unshortened filter would make the bounded relief target arrive AFTER the
      buffer has already slammed the rail — the exact defect the comment at sync.c:2014-2016 blames on
      this filter. Doubling `max_step` alone cannot fix it: step 2 can never outrun step 1);
    firmware/src/sync.c:1960-1999 (`sync_tick_apply_rate` head: `max_sps`, `fast_brake_active`,
      `ramp_dn_sps` are all already in scope at the branch);
    firmware/src/sync.c:494 (`flow_param()` signature) and firmware/src/sync.c:631 (existing
      `flow_param((int)g_extruder_est_sps)` call site — the `baseline_sps` floor D-01 needs);
    firmware/src/sync.c:950-965 (`g_bl_lock_extreme` deepen-then-break-relative pattern — clone its
      shape, not its variable) and firmware/src/sync.c:121, :851 (declaration + reset site);
    firmware/src/sync.c:1143-1200 (`sync_disable`, `sync_rearm_active`) and :1461-1530
      (`sync_tick_auto_start_stop`, the AUTO_START path at :1518) — the reset sites for the new extreme;
    firmware/src/sync_analog.c:48-64 (`psf_control_law`'s soft-wall blend — read it to confirm the
      target arriving at the branch is ALREADY blended toward max_sps; this function is deliberately
      left untouched, D-24);
    firmware/include/sync_internal.h:20-40 (constant block style, `BL_BREAK_DELTA_NORM` definition and
      the `#define X g_runtime_var` alias idiom used for `SYNC_COMPRESSION_COLLAPSE_RAMP_MULT`);
    firmware/src/settings_store.c:201 (`settings_defaults_sync` insertion anchor), :440
      (`tlv_emit_i32(&w, TAG_SYNC_KP_SPS, ...)` emit anchor — note `tlv_emit_f32` exists for floats),
      :767-769 (`settings_load_tlv_tag` case anchor); firmware/include/settings_store.h:59-84 (tag enum;
      re-grep for the current maximum immediately before editing, per 13-RESEARCH.md Pitfall 4);
    firmware/src/settings_store.c:560-610 (`settings_apply_clamps` — note the
      `g_relay_catchup_frac = clamp_f(g_relay_catchup_frac, RELAY_FRAC_MIN, RELAY_FRAC_MAX);` and
      `g_sync_compression_bias_frac = clamp_f(...)` lines: those are float knobs whose TLV `case` at
      settings_store.c:873-885 does a bare `memcpy` with no range check, and the clamp function is what
      makes that safe) and settings_store.c:894-910 (`settings_load_tlv`, whose last statement at :907 is
      `settings_apply_clamps(buf_switch_span_mm);` — that ordering is the seam REVIEW-05 needs);
    firmware/src/protocol.c:533-534 and :1066-1067 (the `SYNC_KP_RATE` GET/SET pair — the persisted,
      release-build template to copy) and firmware/src/protocol.c:1098-1136 (the
      `SYNC_TENSION_STOP_MS` SET block — the `#ifdef FLARE_DEV_TUNING` template to NOT copy,
      13-RESEARCH.md Pitfall 2);
    firmware/src/main.c:170-182 (float knob definition block; note gen_sim_globals.py extracts these
      verbatim into the sim, so a knob defined anywhere else will not link into flare_sim);
    scripts/gen_config.py:114-115 (DEFAULTS entries for the sibling type-P smoothing knobs) and :535-536
      (their `#define CONF_*` emission lines);
    tests/host/sim_scenario.c:36-60 (scenario table head, `steady`/`step_up` shapes) and :570-591
      (`bl_retract_immediate`/`bl_retract_paused` — the rail-scale twin pattern to copy);
    tests/host/sim_trace.c:43-70 (CSV columns: `zone` is `buf_state_name(buf_state_raw())`, `feed_sps` is
      `g_sync_current_sps`, `sat` is the PLANT's physical saturation flag "T"/"C"/"" — `sat` is derived
      from real slack, not from the scaled normalized reading, so it stays valid at any rail scale);
    scripts/test_sync_sim.py:36-60 (scenario name lists) and :785-802
      (`test_type_p_tension_refill_snap` — the test that asserts today's snap behaviour; this task
      inverts it).
  </read_first>
  <behavior>
    Sim-observable expectations, written before the firmware change (they fail against today's snap):
    - `sem_psf_relief_bound` at `--sensor-type p`: for every trace row whose `sat` column is `T`,
      `feed_sps` is strictly less than `CONF_SYNC_MAX_SPS` read from `firmware/include/tune.h`.
    - `sem_psf_relief_bound` emits `SYNC,RELIEF_ON` at least once and emits it fewer than 10 times in the
      run (edge-triggered, not per-tick).
    - `sem_psf_relief_bound`: the largest single-tick increase in `feed_sps` across the run is smaller
      than half of `CONF_SYNC_MAX_SPS` — i.e. the feed ramps rather than stepping the full distance to
      the soft-wall target in one tick.
    - `sem_psf_relief_bound` responsiveness (REVIEW-01): measure extruded filament from the
      `demand_mm_s` column (field 6) integrated over the tick interval. Within one buffer travel
      (`g_buf_max_travel_mm`, 16 mm at the rig default) of extruded filament after the first
      `SYNC,RELIEF_ON` row, `feed_sps` reaches at least 90% of the relief bound computed from the same
      trace (`demand_mm_s` converted to sps times `CONF_SYNC_PSF_RELIEF_MULT`, floored at the lane
      baseline, ceilinged at `CONF_SYNC_MAX_SPS`). This assertion fails against an unshortened 25 mm EMA
      and is the whole point of `SYNC_RELIEF_FILTER_DIV`.
    - `sem_psf_relief_bound_shallow` (`type_p_rail_scale` 0.7): same four assertions hold.
    - `sem_psf_relief_shallow_rail` (`type_p_rail_scale` 0.5): `SYNC,RELIEF_ON` still appears. This is the
      `51bdca8` tripwire — if it does not fire, the trigger is still effectively absolute and the trigger
      must be fixed, never the assertion loosened.
    - `sem_psf_relief_extreme_stale` (REVIEW-07): after one early transient deflection excursion deeper
      than the rail the run settles at, a later starvation in the SAME sync window (no `SYNC,AUTO_START`
      row in between) still emits `SYNC,RELIEF_ON` — at least two `RELIEF_ON` rows total. Without the
      extreme relaxation the second episode never enters the relief zone and this assertion fails.
    - Every scenario in `BASELINE_SCENARIOS` still exits 0 under `--sensor-type p` (no new fault paths).
  </behavior>
  <action>
    Work in this order so the sim assertions exist and fail before the firmware change lands.

    **Knob surface (D-28, persisted template).** Add `"sync_psf_relief_mult": "1.33"` to
    `scripts/gen_config.py`'s `DEFAULTS` dict beside the other type-P smoothing keys, with an inline
    comment naming it as the type-P bounded-relief multiplier, and emit
    `#define CONF_SYNC_PSF_RELIEF_MULT {get_float('sync_psf_relief_mult')}f` in the tune.h generation
    list next to the sibling `CONF_SYNC_PSF_*` lines. Add a matching commented example line to
    `config.ini.example` in the `Buffer Sync` block. Define
    `float g_sync_psf_relief_mult = CONF_SYNC_PSF_RELIEF_MULT;` in `firmware/src/main.c` inside the
    type-P smoothing knob block (it must live in main.c or `gen_sim_globals.py` will not emit it and
    `flare_sim` will fail to link), declare it `extern` in `firmware/include/controller_shared.h` beside
    `g_sync_psf_slew_per_mm`, and seed it in `settings_defaults_sync()` clamped to a physically sane
    band — at least 1.0 (relief below demand is not relief) and at most 3.0 — using the existing
    `clamp_f` idiom. Append a new tag to `settings_tag_t`: re-grep `TAG_` in
    `firmware/include/settings_store.h` first and use the next free slot after the current maximum, then
    add the matching `tlv_emit_f32` line in `settings_save()` and the matching `case` in
    `settings_load_tlv_tag()` (float payload, `sizeof(float)` length check).

    **Clamp the load path, not just the set path (REVIEW-05).** The TLV `case` is a bare `memcpy` by
    project convention, so a corrupt sector, a truncated write, or a record produced by a different
    firmware build lands an unconstrained float straight in the control loop. Do not add a range check
    inside `settings_load_tlv_tag()` — that would diverge from every existing float tag. Instead add
    `g_sync_psf_relief_mult = clamp_f(g_sync_psf_relief_mult, SYNC_RELIEF_MULT_MIN, SYNC_RELIEF_MULT_MAX);`
    to `settings_apply_clamps()` beside the existing `g_relay_catchup_frac` / `g_sync_compression_bias_frac`
    clamp lines, and define those two bounds as `1.0f` and `3.0f` in the same anonymous-enum/const block
    `RELAY_FRAC_MIN`/`RELAY_FRAC_MAX` live in so the SET handler, the defaults seed and the post-load
    clamp all cite one pair of names rather than three copies of the literals. `settings_load_tlv()` calls
    `settings_apply_clamps()` as its final statement, so this covers the flash path for free; the v63
    struct path at settings_store.c:979 calls it too.

    Do NOT add a field to
    `settings_v63_t` and do NOT bump `SETTINGS_VERSION` — the v63 struct is the frozen legacy layout and
    new knobs ride the TLV path only. Add release-build `GET:SYNC_PSF_RELIEF_MULT` and
    `SET:SYNC_PSF_RELIEF_MULT` handlers in `protocol.c` modelled on the `SYNC_KP_RATE` pair, clamping the
    parsed float to the same `SYNC_RELIEF_MULT_MIN`/`SYNC_RELIEF_MULT_MAX` band; these handlers must sit
    outside every `#ifdef FLARE_DEV_TUNING` region.

    **Sim scenarios and assertions first (RED).** Add four entries to `g_sim_scenarios` in
    `tests/host/sim_scenario.c`, all `.type_specific = true` with `p` implied by their `psf` name, all
    `.active_lane = 1, .start_sync_active = true`: `sem_psf_relief_bound` (a `DEMAND_STEP_UP` profile
    whose second level is high enough to pull the buffer into the tension zone and hold it there for
    several hundred ticks — copy `step_up`'s shape and raise `level2_mm_s` until the trace actually shows
    `sat` of `T` rows), `sem_psf_relief_bound_shallow` (identical, plus `.type_p_rail_scale = 0.7f`),
    `sem_psf_relief_shallow_rail` (identical, plus `.type_p_rail_scale = 0.5f`), and
    `sem_psf_relief_extreme_stale` (REVIEW-07's tripwire: a profile that drives one deep transient
    deflection excursion early — deeper than the rail the run subsequently settles at — then recovers well
    off it, then starves again later in the same sync window with no AUTO_STOP/AUTO_START edge in between.
    Use `demand_gain`/`feed_gain` shaping rather than a new plant field; confirm from the trace that the
    early excursion really is the deepest `bp_mm` of the run and that no `SYNC,AUTO_START` row appears
    after it). Give each a comment naming the decision or review finding it defends. In
    `scripts/test_sync_sim.py`, add a module-level helper
    `_conf_int(name)` that regex-reads a `#define CONF_<name> <int>` value out of
    `firmware/include/tune.h` (same regex-the-C-source idiom `scripts/test_settings_parity.py` already
    uses) and a sibling `_conf_float(name)` for `CONF_SYNC_PSF_RELIEF_MULT` and
    `CONF_SYNC_PSF_FILTER_MM`, add the four scenario names to the type-P scenario list, and add a new
    test class implementing every Behavior bullet above using those helpers rather than hardcoded
    numbers. The responsiveness bullet needs one extra piece: a small helper that walks the trace rows
    from the first `SYNC,RELIEF_ON` accumulating `demand_mm_s * dt` so the assertion is expressed in
    millimetres of extruded filament, not ticks — a tick-count assertion would silently pass at low flow,
    which is exactly the regime the 25 mm EMA is slowest in. Build and run them; confirm BOTH the bound
    assertions and the responsiveness assertion fail against the current firmware before continuing (the
    bound fails because of the snap, the responsiveness assertion must be re-confirmed as failing after
    the snap is removed but before the filter divisor lands — record both observations in the SUMMARY).

    **Control loop (GREEN).** In `firmware/include/sync_internal.h`, add four T3 constants beside
    `BL_BREAK_DELTA_NORM` with doc comments: `SOFT_WALL_MARGIN_NORM` at `0.15f` (D-29 delegates the exact
    value within 0.10-0.20; 0.15 sits mid-band and is under `BL_BREAK_DELTA_NORM` so entering relief is
    strictly easier than declaring a BL break), `SYNC_RELIEF_SLEW_MULT` at `2.0f` (D-02's doubled slew
    cap), `SYNC_RELIEF_FILTER_DIV` at `4.0f` (REVIEW-01: the divisor applied to `filter_len_mm` while
    relieving, taking the effective EMA length from 25 mm to about 6 mm — comfortably inside one 16 mm
    buffer travel, which is what makes a bounded relief target arrive before the rail does), and
    `SYNC_EXTREME_RELAX_MULT` at `2.0f` (REVIEW-07: how many `BL_BREAK_DELTA_NORM` above the recorded
    extreme the buffer must be observed before the extreme re-seeds). Document at
    `SYNC_EXTREME_RELAX_MULT` that it is deliberately greater than 1.0 so the relaxation threshold sits
    strictly above the probe's CONSUMER threshold in plan 13-03 — the extreme can never relax without the
    probe already having grounds to call CONSUMER, so the two rules cannot disagree.

    In `firmware/src/sync.c`, add two file-scope statics beside `g_bl_lock_extreme`:
    `g_sync_tension_extreme` (float) and `g_sync_tension_extreme_valid` (bool), plus
    `g_sync_relief_active` (bool) for edge detection. Track the extreme only while `sync_enabled` and
    `g_buf_sensor_type == BUF_SENSOR_TYPE_P` and the debounced state is `BUF_TENSION`: deepen it toward
    the more negative reading and set the validity flag, exactly mirroring the `g_bl_lock_extreme` block.
    Clear all three (extreme to 0.0f, both flags false) in `sync_disable()`, in `sync_rearm_active()`, and
    on the AUTO_START path in `sync_tick_auto_start_stop()` — a stale deep extreme from a previous window
    would make the relief zone unreachable and silently disable relief.

    **Bound the extreme's staleness within a window (REVIEW-07).** Those three resets are per sync window;
    a long single-material print can run for hours without hitting any of them, so one uncalibrated deep
    deflection spike would depress the relief threshold for the rest of the print and silently under-fire
    relief — the same silent-disable family as 51bdca8, just reached by a different route. In the same
    tracking block, after the deepen step, add a relaxation: while the validity flag is set, if `g_buf_pos`
    is observed at or above `g_sync_tension_extreme + SYNC_EXTREME_RELAX_MULT * BL_BREAK_DELTA_NORM`,
    re-seed `g_sync_tension_extreme` to the current `g_buf_pos`. Evaluate the relaxation on every type-P
    tick while sync is enabled, not only in `BUF_TENSION` — the recovery it is watching for happens
    outside the tension zone by definition. Keep it rail-relative and clock-free: no wall-clock decay, no
    tick counter, no numeric position literal. Note in a comment that the relaxation can only ever make
    the relief zone easier to enter, so its failure direction is "relief fires slightly more often",
    which the existing `RELIEF_ON` fewer-than-10-per-run assertion already bounds.

    Add a helper `static int sync_type_p_relief_bound_sps(int max_sps)` implementing D-01: take
    `(int)g_extruder_est_sps` scaled by `g_sync_psf_relief_mult`, raise it to
    `flow_param((int)g_extruder_est_sps).baseline_sps` if that is larger, fall back to `g_sync_min_sps`
    only when the result is still non-positive, and finally clamp to `max_sps`. Add a second helper
    `static bool sync_type_p_in_relief_zone(void)` implementing D-22/D-23: when the validity flag is set,
    return true iff `g_buf_pos` is at or below the tracked extreme plus `SOFT_WALL_MARGIN_NORM`;
    otherwise return true iff `g_buf.state == BUF_TENSION`. It must contain no numeric normalized-position
    literal of its own.

    Rewrite the type-P arm of the branch chain at `sync_tick_apply_rate` so its guard is
    `g_buf_sensor_type == BUF_SENSOR_TYPE_P && sync_type_p_in_relief_zone()`, and its body: computes the
    bound via the new helper, takes the smaller of the bound and the incoming target, and assigns the
    result of the smoothing path to the applied step rate — the branch no longer writes the incoming
    target into the applied step rate at all. Extend `sync_apply_type_p_smoothing` with two extra
    parameters: the slew allowance to use in place of the direct `g_sync_psf_slew_per_mm` read, and the
    EMA length to use in place of the direct `g_sync_psf_filter_mm` read. The ordinary type-P branch
    passes `g_sync_psf_slew_per_mm` and `g_sync_psf_filter_mm` unchanged — its behaviour must be
    bit-identical to today. The relief branch passes the slew scaled by `SYNC_RELIEF_SLEW_MULT` and the
    filter length divided by `SYNC_RELIEF_FILTER_DIV`.

    That filter-length parameter is REVIEW-01's fix and is not optional: step 1 of the smoothing function
    EMAs the target over `filter_len_mm`, which defaults to 25 mm, so on the rig's 16 mm buffer an
    unshortened filter makes the bounded relief target arrive after the buffer has already hit the rail —
    doubling `max_step` in step 2 cannot outrun a throttled step 1. Keep the existing
    `PSF_FILTER_MIN_MM` floor applied to the passed-in value (the divisor must not be able to produce a
    zero or negative EMA length), keep the `g_psf_target_filt` state single and shared across both
    branches (one filter state, two parameter sets — do not add a second filtered-target variable), and
    add a comment at the divisor's use site recording that shortening the EMA in relief is what keeps
    plan 13-03's probe from reading a slow ramp as a missing consumer.

    Keep the slew strictly distance-clocked and keep the existing 1.0f minimum
    step — do not add any wall-clock minimum on the UP direction (D-03; memory
    `typep-sync-overfeed-frozen-clock` documents why a frozen refill on a stopped extruder is correct).
    Seed `g_psf_target_filt` from `g_extruder_est_sps` exactly once, on the transition into relief, not
    every tick — seeding every tick would defeat the EMA the bound is supposed to run through.

    On that same transition emit `cmd_event("SYNC", "RELIEF_ON")`, and on the transition out emit
    `cmd_event("SYNC", "RELIEF_OFF")`, driven off `g_sync_relief_active` so neither can fire per tick
    (D-06). Add no new fixed-position `ST:` field for relief.

    Leave `psf_control_law` in `firmware/src/sync_analog.c` completely untouched (D-24) and record in the
    task's SUMMARY that making its blend rail-relative is a deliberate follow-up carried out of this
    phase. Leave the compression side alone entirely (D-05).
  </action>
  <verify>
    <automated>python3 scripts/gen_config.py &amp;&amp; ninja -C build_local &amp;&amp; ninja -C build_sim &amp;&amp; build_sim/flare_sim --scenario sem_psf_relief_bound --sensor-type p | tail -3 &amp;&amp; python3 -m unittest scripts.test_settings_parity -v</automated>
    <fails_when>non-zero exit code from any stage; or the unittest output contains "FAIL settings parity"; or the flare_sim invocation prints nothing on stdout (scenario name not found in g_sim_scenarios)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*[/*]' firmware/src/sync.c | grep -c 'CONF_PSF_SOFT_WALL_START'` prints `0` — the absolute soft-wall literal is gone from the control loop. `grep -c 'CONF_PSF_SOFT_WALL_START' firmware/src/sync_analog.c` is still `>= 2` (D-24 leaves the blend alone).
    - `grep -v '^[[:space:]]*[/*]' firmware/src/sync.c | grep -c 'g_sync_current_sps = target_sps'` prints `0` — the direct-apply bypass is removed.
    - `grep -c 'SOFT_WALL_MARGIN_NORM' firmware/include/sync_internal.h` is `>= 1`,
      `grep -c 'SYNC_RELIEF_SLEW_MULT' firmware/include/sync_internal.h` is `>= 1`,
      `grep -c 'SYNC_RELIEF_FILTER_DIV' firmware/include/sync_internal.h` is `>= 1`, and
      `grep -c 'SYNC_EXTREME_RELAX_MULT' firmware/include/sync_internal.h` is `>= 1`.
    - REVIEW-01 fix is wired, not just declared: `grep -c 'SYNC_RELIEF_FILTER_DIV' firmware/src/sync.c` is `>= 1`, and `sed -n '/static int sync_apply_type_p_smoothing/,/^}/p' firmware/src/sync.c | grep -v '^[[:space:]]*[/*]' | grep -c 'g_sync_psf_filter_mm'` prints `0` — the function reads its EMA length from its parameter, not from the global, so the relief branch can shorten it.
    - REVIEW-05 fix is on the load path: `sed -n '/static void settings_apply_clamps/,/^}/p' firmware/src/settings_store.c | grep -c 'g_sync_psf_relief_mult'` is `>= 1`.
    - REVIEW-07 relaxation exists and is rail-relative: `grep -c 'SYNC_EXTREME_RELAX_MULT' firmware/src/sync.c` is `>= 1`, and `build_sim/flare_sim --scenario sem_psf_relief_extreme_stale --sensor-type p | grep -c 'SYNC,RELIEF_ON'` is `>= 2` (relief still fires on the second starvation after a deep one-off spike).
    - `grep -c 'g_sync_psf_relief_mult' firmware/src/main.c` is `>= 1` (definition present where
      `gen_sim_globals.py` can see it) and `grep -c 'g_sync_psf_relief_mult' firmware/include/controller_shared.h` is `>= 1`.
    - `awk '/#ifdef FLARE_DEV_TUNING/{d=1} /#endif/{d=0} d &amp;&amp; /SYNC_PSF_RELIEF_MULT/{n++} END{print n+0}' firmware/src/protocol.c` prints `0` — neither new handler is dev-tuning-gated (13-RESEARCH.md Pitfall 2).
    - `grep -c 'TAG_SYNC_PSF_RELIEF_MULT' firmware/include/settings_store.h firmware/src/settings_store.c` shows the tag in the header once and in `settings_store.c` twice (emit + load case).
    - `python3 -m unittest scripts.test_settings_parity -v` reports `OK` on the last line.
    - `build_sim/flare_sim --scenario sem_psf_relief_shallow_rail --sensor-type p | grep -c 'SYNC,RELIEF_ON'` is `>= 1` (the 51bdca8 tripwire).
    - `python3 -m unittest scripts.test_sync_sim.TypePReliefBoundTests -v` (or whatever the new class is named — name it in the SUMMARY) reports `OK`.
  </acceptance_criteria>
  <done>The bounded relief path is live end-to-end: `SYNC_PSF_RELIEF_MULT` round-trips through flash and `GET:` in a release build and is re-clamped by `settings_apply_clamps()` after every load, relief entry is decided rail-relative with no absolute literal left in `sync.c` and its extreme cannot go stale within a window, relief feed climbs through the shortened-filter doubled-slew smoothing path and reaches 90% of its bound within one buffer travel, `RELIEF_ON`/`RELIEF_OFF` bracket each episode once, and the bound invariant holds at rail scales 1.0, 0.7 and 0.5.</done>
  <reversibility rating="costly">Removing the direct-apply bypass (D-02) changes the type-P feed law's dynamic response, and adding a persisted TLV tag (D-28) makes `SYNC_PSF_RELIEF_MULT` part of the settings schema and the `--dump` parity surface — retiring the tag later is a schema change, and a rig that has already flash-saved the new tag will keep it across a firmware downgrade. Both are explicitly accepted in 13-CONTEXT.md D-02/D-28. Code-wise still a plain git revert.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cap the tension ramp path at the same bound and prove the invariant is unconditional</name>
  <files>firmware/src/sync.c, tests/host/sim_scenario.c, scripts/test_sync_sim.py</files>
  <read_first>
    firmware/src/sync.c:1638-1673 (`sync_check_tension_dwell_and_ramp` — the `g_sync_tension_ramp_delay_ms`
      branch at the end raises `target_sps` to the clamped max unconditionally once the ramp delay
      elapses; that is the second path D-04 says must be capped);
    firmware/src/sync.c:1759+ (`sync_tick_calculate_target` — confirm the ramp helper's return value flows
      into `sync_tick_apply_rate` and therefore through Task 1's branch, and decide whether capping at the
      ramp site or at the apply site is the single place that covers both);
    firmware/include/tune_internal.h (grep `FLARE_INT_SYNC_TENSION_RAMP_DELAY_MS` — this knob is
      dev-tuning-gated, so the ramp path is invisible to a default build and to clang-tidy: memory
      `dev-tuning-build-blindspot`);
    tests/host/sim_scenario.c (the three scenarios added in Task 1 — extend, do not replace).
  </read_first>
  <behavior>
    - With the tension ramp delay elapsed and the buffer still pegged, `feed_sps` on `sat == "T"` rows
      still stays strictly below `CONF_SYNC_MAX_SPS` — capping the urgent-refill branch alone is not
      sufficient if the ramp path can raise the target past the bound.
    - The invariant assertion runs over all three scenarios from Task 1 in a single parametrised test, so
      adding a fourth rail scale later is a one-line change.
    - `python3 -m unittest scripts.test_sync_sim -v` shows no scenario that previously exited 0 now exits
      non-zero.
  </behavior>
  <action>
    Cap the tension-dwell ramp so it can never raise the commanded target above the relief bound while the
    type-P buffer is in the relief zone (D-04). Prefer a single cap applied where the bound is already
    computed over duplicating the ramp logic: if the apply-side branch from Task 1 already takes the
    smaller of bound and target, verify by trace whether the ramp path can still push a value past it (it
    can, when the ramp raises the target while the buffer sits in `BUF_TENSION` but outside the
    extreme-relative relief zone) and cap at whichever single site closes that hole. Guard the cap on
    `g_buf_sensor_type == BUF_SENSOR_TYPE_P` so type-D's ramp behaviour is unchanged, and leave the
    existing `g_buf_sensor_type != BUF_SENSOR_TYPE_D` fault-hold guard in that function alone.

    Because `g_sync_tension_ramp_delay_ms` is a `FLARE_DEV_TUNING` knob, configure and build the dev
    superset before judging this task complete — a default build compiles neither the SET/GET nor,
    potentially, the code paths reachable only when the knob is non-default.

    Refactor the Task 1 bound assertions in `scripts/test_sync_sim.py` into one parametrised test that
    loops over the three scenario names via `subTest`, so the invariant is stated once and applied to
    every rail scale. Add one scenario, `sem_psf_relief_ramp_capped`, that runs long enough for the
    tension ramp delay to elapse while the buffer is still pegged (check the effective
    `FLARE_INT_SYNC_TENSION_RAMP_DELAY_MS` value and set `tick_ceiling` accordingly, with a
    `tick_ceiling_reason` string since the table requires one whenever the ceiling is non-default), plus
    its `_shallow` twin at `type_p_rail_scale` 0.7 per D-25, and include both in the parametrised loop.
  </action>
  <verify>
    <automated>cmake -S firmware -B build_local -G Ninja -DFLARE_DEV_TUNING=ON &amp;&amp; ninja -C build_local &amp;&amp; ninja -C build_sim &amp;&amp; python3 -m unittest scripts.test_sync_sim -v 2>&amp;1 | tail -20</automated>
    <fails_when>non-zero exit code from cmake/ninja; or the unittest tail contains "FAILED" or "ERROR"; or the tail does not end with a line beginning "OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `build_sim/flare_sim --scenario sem_psf_relief_ramp_capped --sensor-type p | awk -F, '$8=="T"{print $4}' | sort -n | tail -1` prints a value strictly less than the `CONF_SYNC_MAX_SPS` value in `firmware/include/tune.h` (field 8 is `sat`, field 4 is `feed_sps` — confirm the column indices against `tests/host/sim_trace.c:44` before relying on them).
    - `grep -c 'sem_psf_relief_ramp_capped' tests/host/sim_scenario.c scripts/test_sync_sim.py` shows the scenario registered in both files.
    - `python3 -m unittest scripts.test_sync_sim -v 2>&amp;1 | tail -1` begins with `OK`.
  </acceptance_criteria>
  <done>Both paths that can raise type-P feed while pegged at tension are capped by the same bound; the invariant test is parametrised across five scenarios spanning rail scales 1.0, 0.7 and 0.5; the dev-tuning superset builds clean.</done>
  <reversibility rating="reversible">A single additional clamp guarded on the type-P sensor type; type-D behaviour untouched. Plain git revert.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Retire the snap expectation, finish the knob's public surface, register requirements</name>
  <files>scripts/test_sync_sim.py, scripts/flare_cmd.py, MANUAL.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    scripts/test_sync_sim.py:785-802 (`test_type_p_tension_refill_snap` — the requirement it cites,
      "Type-P Tension Refill Snap (psf-type-p-sensor spec)", is the behaviour Phase 13 SC#1 deliberately
      replaces; the test must be rewritten to assert the new contract, not deleted);
    scripts/test_sync_sim.py:126-150 (the scenario-list-driven tests — check whether any existing type-P
      scenario's expected trace shifts once the snap is gone, per 13-CONTEXT.md "Claude's Discretion":
      same pass/fail outcome is the bar, trace shape may change);
    scripts/flare_cmd.py:61-120 (`DUMP_PARAMS` table format: `(SET/GET name, config.ini key, lane_aware)`)
      and scripts/test_settings_parity.py:135-150 (`test_dump_keys_rebuild_with_gen_config` — the gate that
      requires every dumped key to be a key `gen_config.py` accepts);
    MANUAL.md:186-200 (the `SET:`/`GET:` parameter table row format: command, config key, description,
      default) and MANUAL.md:353-370 (the `EV:` event table, `SYNC` row — the pipe-separated event
      vocabulary that needs the two new names);
    .planning/REQUIREMENTS.md (the `### REQ-ID` / `- **Source**:` / `- **Description**:` entry format —
      match the `REQ-klipper-status-parity-*` block added by Phase 14);
    .planning/ROADMAP.md (the `### Phase 13:` block — its `**Requirements**:` line currently reads TBD and
      its `**Plans**:` line reads TBD).
  </read_first>
  <action>
    Rewrite `test_type_p_tension_refill_snap` so it asserts the Phase 13 contract instead of the retired
    one: the type-P buffer entering the tension zone must still produce a prompt feed increase (relief is
    not disabled — assert the feed rises measurably within a bounded number of ticks after the first
    `SYNC,RELIEF_ON`), but must never reach the value returned by `_conf_int("SYNC_MAX_SPS")` on a row the
    plant marks tension-saturated. Rename the test to name the new behaviour, keep a comment recording
    that it supersedes the `psf-type-p-sensor` snap requirement and why (Phase 13 SC#1), and remove the
    hardcoded step-rate literal in favour of `_conf_int`. Then run the whole suite and fix any other
    type-P scenario whose expectation genuinely encoded the snap; a changed trace shape with an unchanged
    pass/fail outcome needs no edit.

    Add `("SYNC_PSF_RELIEF_MULT", "sync_psf_relief_mult", False)` to `DUMP_PARAMS` in
    `scripts/flare_cmd.py`, in the buffer-sync region of the table, so `--dump` emits it and the dumped
    config still rebuilds (D-28's `--dump` touchpoint; the parity test enforces the gen_config side).

    Document the knob in `MANUAL.md`: one row in the `SET:`/`GET:` parameter table giving the command
    name, the `sync_psf_relief_mult` config key, a description that states it is the type-P bounded-relief
    multiplier applied to the estimated extruder demand, that the result is floored at the learned
    per-lane baseline and ceilinged at the clamped max rate, that it is live and flash-persisted, and its
    clamp band; and the default `1.33`. Extend the `SYNC` row of the `EV:` event table with the two new
    event names and a clause explaining that they bracket a bounded-relief episode (entry/exit edges only,
    not per tick) and are distinct from the pre-existing relief-pause event. Do not add the knob to the
    dev-tuning-gated key list in the MANUAL note — it is deliberately not gated.

    Register six requirement IDs in `.planning/REQUIREMENTS.md` using the existing entry format, with
    Source set to `.planning/phases/13-type-p-sync-relief-fault-trip/13-RESEARCH.md` for all six:
    `REQ-type-p-sync-relief-bounded-refill`, `REQ-type-p-sync-relief-distance-trip`,
    `REQ-type-p-sync-relief-feed-probe`, `REQ-type-p-sync-relief-rail-relative-triggers`,
    `REQ-type-p-sync-relief-sim-coverage`, `REQ-type-p-sync-relief-no-relay-reintroduction`. Use the
    descriptions from 13-RESEARCH.md's "Phase Requirements" table verbatim. Replace the placeholder text
    on the Phase 13 `**Requirements**:` line in `.planning/ROADMAP.md` with the six IDs comma-separated,
    and replace the placeholder on its `**Plans**:` line with the four-plan wave listing for this phase
    (13-01 wave 1, 13-02 wave 2, 13-03 wave 3, 13-04 wave 4). Leave no placeholder text behind in the
    Phase 13 block.

    Record in the SUMMARY the commit SHA that immediately precedes this phase's first commit, labelled as
    the phase-start SHA — plans 13-03 and 13-04 diff against it to prove the type-D relay path was never
    touched (SC#5) and to scope the rig-validation diff.

    Finish with the full repository gate and record its result in the SUMMARY, including the
    `python3 -m unittest scripts.test_sync_sim -v` pass count so plans 02 and 03 have a concrete
    no-regression baseline to diff against (13-RESEARCH.md Assumption A1 — do not trust ROADMAP's literal
    "16 PSF scenarios", measure it).
  </action>
  <verify>
    <automated>python3 scripts/validate_regression.py 2>&amp;1 | tail -25</automated>
    <fails_when>non-zero exit code; or the output contains "FAILED", "Traceback", or "scenario suite failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sync_psf_relief_mult' scripts/flare_cmd.py scripts/gen_config.py config.ini.example MANUAL.md` shows a match in all four files.
    - `python3 -m unittest scripts.test_settings_parity -v 2>&amp;1 | tail -1` begins with `OK` (the `--dump` rebuild gate accepts the new key).
    - `grep -c 'REQ-type-p-sync-relief-' .planning/REQUIREMENTS.md` is `>= 6`.
    - `sed -n '/^### Phase 13:/,/^### Phase 14:/p' .planning/ROADMAP.md | grep -c 'TBD'` prints `0`.
    - `grep -v '^[[:space:]]*#' scripts/test_sync_sim.py | grep -c '15004'` prints `0` — no hardcoded max-rate literal survives in the type-P tests.
    - `python3 scripts/validate_regression.py` exits 0 and its host-sim stage reports `OK`; the SUMMARY records the observed `test_sync_sim` test count.
  </acceptance_criteria>
  <done>No test still asserts the retired snap behaviour; `SYNC_PSF_RELIEF_MULT` has all seven touchpoints including `--dump` and MANUAL.md; the two new events are documented; six REQ IDs are registered and ROADMAP Phase 13 no longer says TBD; the full regression gate is green and the sim baseline count is recorded.</done>
  <reversibility rating="reversible">Test expectations, a dump-table row, docs and planning metadata. Plain git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host `SET:` over USB CDC → `protocol.c` parameter handler | Any process with the serial port open (or Moonraker write access to the FLARE daemon fronting it) can set the new relief multiplier at runtime. Single-client serial device, no firmware-layer auth concept; daemon-layer auth is Phase 9's scope. |
| Flash TLV record → `settings_load_tlv_tag()` | A persisted settings sector written by a different firmware build (or corrupted) is parsed at boot; a duplicate or mis-sized tag silently overwrites an unrelated field, and the `case` bodies are bare `memcpy` with no range check by project convention. |
| Plant/ADC reading → `g_buf_pos` → relief trigger | The normalized buffer reading is uncalibrated per rig; a threshold derived from it can silently disable a safety behaviour on a rig whose rail reads shallow (the `51bdca8` failure mode). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | `SET:SYNC_PSF_RELIEF_MULT` handler | medium | mitigate | Clamp the parsed float to 1.0-3.0 in both the SET handler and `settings_defaults_sync()`, matching the `clamp_i`/`clamp_f`-on-set pattern every other handler in `protocol.c` uses. The outer `min(max_sps, ...)` in `sync_type_p_relief_bound_sps()` is a second, independent ceiling, so even an unclamped multiplier cannot command above the configured max rate. |
| T-13-02 | Denial of Service (mechanical) | Bounded relief target vs. the tension hard stop | high | mitigate | The whole point of the change: the relief target is bounded by demand × multiplier and floored at the learned baseline, so a sustained pegged episode can no longer command the raw max rate into a hard stop. Task 1's invariant is asserted unconditionally in sim at three rail scales; plan 13-04's HW pass bar item 4 (no rail hits during print sync, expressed rail-relative) is the physical confirmation. |
| T-13-19 | Denial of Service (under-responsive relief) | Bounded relief target routed through the unshortened 25 mm target EMA | high | mitigate | REVIEW-01. Bounding the target only helps if the bound is reached in time; a 25 mm EMA on a 16 mm buffer is slower than the starvation it answers, so the relief branch divides `filter_len_mm` by `SYNC_RELIEF_FILTER_DIV`. Enforced by the Task 1 responsiveness Behavior bullet (90% of bound within one buffer travel of extruded filament, measured from `demand_mm_s`, not from tick counts) and by an acceptance criterion proving `sync_apply_type_p_smoothing` takes its EMA length as a parameter rather than reading the global. Downstream impact if unfixed: plan 13-03's probe sees a slow ramp and misreads it as a missing consumer. |
| T-13-20 | Tampering | Corrupt / foreign flash sector supplying an out-of-range `SYNC_PSF_RELIEF_MULT` | medium | mitigate | REVIEW-05. The TLV `case` is a bare `memcpy` by project convention, so the clamp lives in `settings_apply_clamps()` — which `settings_load_tlv()` calls as its final statement (settings_store.c:907) and the v63 path calls at :979 — beside the existing `g_relay_catchup_frac` clamp. Bounds are named constants (`SYNC_RELIEF_MULT_MIN`/`_MAX`) shared by the SET handler, the defaults seed and the post-load clamp. Residual impact is capped anyway by the independent `min(max_sps, ...)` ceiling in `sync_type_p_relief_bound_sps()`, which is why this is medium and not high. |
| T-13-21 | Denial of Service (silent feature disable) | Stale `g_sync_tension_extreme` depressing the relief threshold across a long print | medium | mitigate | REVIEW-07. A single uncalibrated deep spike would make the relief zone unreachable until the next AUTO_START — hours on a single-material print. Mitigated by the rail-relative relaxation (re-seed once the buffer is observed at `extreme + SYNC_EXTREME_RELAX_MULT * BL_BREAK_DELTA_NORM`), which adds no clock and no position literal, and by the `sem_psf_relief_extreme_stale` scenario asserting relief still fires on a second starvation in the same window. Failure direction of the mitigation itself is "relief fires slightly more often", already bounded by the fewer-than-10 `RELIEF_ON` assertion. |
| T-13-03 | Tampering | New TLV tag number colliding with an existing tag | high | mitigate | Task 1 re-greps `TAG_` in `settings_store.h` for the current maximum immediately before appending, and `scripts/test_settings_parity.py` enforces emit/load pairing. Note the parity test does NOT catch two tags sharing a numeric value — the re-grep is the actual control (13-RESEARCH.md Pitfall 4). |
| T-13-04 | Denial of Service (silent feature disable) | Rail-relative relief trigger on a shallow-reading rig | high | mitigate | `sync_type_p_in_relief_zone()` carries no numeric normalized-position literal, falls back to the debounced `BUF_TENSION` state (itself goal-relative, `sync_buf.c:484-495`) before an extreme exists, and the `sem_psf_relief_shallow_rail` tripwire at `type_p_rail_scale` 0.5 fails the build if relief stops firing. |
| T-13-05 | Repudiation | Relief episodes invisible in rig logs | low | mitigate | `RELIEF_ON`/`RELIEF_OFF` edge events give every episode a bracketed start and end in the `verify_hw_open_items.py watch` capture, which is what plan 13-04's D-31 pass bar is measured from. |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No external packages are installed by this phase — firmware C plus existing stdlib-only host scripts. 13-RESEARCH.md marks the Package Legitimacy Audit not-applicable for the same reason. |
</threat_model>

<verification>
- `python3 scripts/validate_regression.py` green end to end (gen_config, dev-superset firmware build, sim build, sim suite, py_compile, ruff).
- `python3 -m unittest scripts.test_settings_parity -v` green — proves the new TLV tag is both emitted and loaded and that `--dump` still rebuilds.
- Manual read of the diff against `REVIEW.md` before the final commit (AGENTS.md rule 13), specifically checking the naming rule (`g_lower_case` globals) and that no `HW:` item anywhere was checked off (AGENTS.md rule 12).
</verification>

<success_criteria>
- Phase 13 Success Criterion 1 is met and machine-checked: type-P urgent refill targets a bounded value routed through the distance-EMA/slew path with a doubled slew cap, never the raw clamped max.
- The ROADMAP carried caveat is honoured: `sync.c` contains zero absolute normalized-position literals for relief entry, and `EV:SYNC:RELIEF_ON` still fires at `type_p_rail_scale` 0.5.
- `SYNC_PSF_RELIEF_MULT` is live, flash-persisted, release-build exposed, dumpable and documented (all seven touchpoints).
- The six phase REQ IDs exist in `.planning/REQUIREMENTS.md` and ROADMAP Phase 13 no longer reads TBD.
- A concrete `scripts.test_sync_sim` pass count is recorded in the SUMMARY as the no-regression baseline for plans 02-03.
</success_criteria>

<output>
Create `.planning/phases/13-type-p-sync-relief-fault-trip/13-01-SUMMARY.md` when done.
</output>

## Review Dispositions

Cross-AI review source: `13-REVIEWS.md` (antigravity lane, 2026-09-12). Every finding that names plan
13-01 is listed here with its disposition. Findings addressed to 13-02/13-03/13-04 are dispositioned in
those plans.

REVIEW-NN labels are this phase's stable handles for the findings and are the strings that appear in
source comments, threat rows and scenario comments. The map across all four plans is fixed:
REVIEW-01 13-01 EMA lag - REVIEW-02 13-03 point-in-time probe sample - REVIEW-03 13-03 threshold
inversion - REVIEW-04 13-02 hold falling edge - REVIEW-05 TLV clamp (13-01 and 13-02 halves) -
REVIEW-06 13-02 cannot_refill dormancy - REVIEW-07 13-01 extreme staleness - REVIEW-08 13-03 PROBE:
guard conditions - REVIEW-09 13-04 log-parsing automation.

| Finding | Severity | Disposition | Rationale |
|---------|----------|-------------|-----------|
| **REVIEW-01** — Target EMA lag during urgent relief: the bounded relief target is filtered through `sync_apply_type_p_smoothing()`'s 25 mm distance EMA (`CONF_SYNC_PSF_FILTER_MM`) before slew limiting, so on the rig's 16 mm buffer the bound arrives after the rail does; doubling `max_step` cannot outrun a throttled step 1. | HIGH | **incorporated** | `SYNC_RELIEF_FILTER_DIV` (4.0f) added in Task 1: `sync_apply_type_p_smoothing()` now takes its EMA length as a parameter, the relief branch passes `filter_len_mm / SYNC_RELIEF_FILTER_DIV`, the ordinary branch stays bit-identical. Enforced by the Task 1 responsiveness Behavior bullet (90% of bound within one buffer travel of extruded filament, measured off `demand_mm_s`), an acceptance criterion proving the function no longer reads `g_sync_psf_filter_mm` directly, a `must_haves.truths` entry, and threat `T-13-19`. |
| **REVIEW-05 (13-01 half)** — TLV deserializer clamp omission: `settings_load_tlv_tag()` `memcpy`s float payloads with no range check, so a corrupt or foreign flash sector can land an unconstrained `SYNC_PSF_RELIEF_MULT` in the control loop. | MEDIUM | **incorporated** | Task 1 adds `g_sync_psf_relief_mult = clamp_f(..., SYNC_RELIEF_MULT_MIN, SYNC_RELIEF_MULT_MAX)` to `settings_apply_clamps()` (settings_store.c:560-610) rather than inside the TLV `case` — matching the existing `g_relay_catchup_frac` / `g_sync_compression_bias_frac` treatment and covering both the TLV path (called at settings_store.c:907) and the v63 path (:979). Bounds are named constants shared by the SET handler, the defaults seed and the post-load clamp. `must_haves.truths` + `key_links` + threat `T-13-20`. |
| **REVIEW-07** — Long-window extreme staleness: `g_sync_tension_extreme` resets only on `sync_disable()` / `sync_rearm_active()` / AUTO_START, so a single uncalibrated deep spike depresses the relief threshold for the rest of a long print. | LOW | **incorporated** | Task 1 adds `SYNC_EXTREME_RELAX_MULT` (2.0f) and a rail-relative re-seed: once `g_buf_pos` is observed at or above `extreme + SYNC_EXTREME_RELAX_MULT * BL_BREAK_DELTA_NORM`, the extreme re-seeds to the current reading. Clock-free, no position literal, evaluated on every type-P tick. Tripwire scenario `sem_psf_relief_extreme_stale` asserts a second `RELIEF_ON` in the same sync window. `must_haves.truths` + threat `T-13-21`. Raised above LOW handling because its failure mode is the silent-disable family `51bdca8` belongs to. |
| **Cross-plan coherence #1** — relief response vs. filter lag couples 13-01 to 13-03: a slow relief ramp makes 13-03's probe read a working consumer as missing. | (context) | **incorporated** | Same fix as REVIEW-01; the coupling is recorded verbatim in `must_haves.key_links` and in `T-13-19`'s downstream-impact clause so a future edit to the divisor cannot silently break the probe in 13-03. |
| **Cross-plan coherence #3** — TLV tag sequence: 13-01 and 13-02 each append a tag and must not collide. | (context) | **incorporated (pre-review)** | Already covered: Task 1's action requires re-grepping `TAG_` in `settings_store.h` for the current maximum immediately before appending (13-RESEARCH.md Pitfall 4), threat `T-13-03` names the re-grep as the actual control, and 13-02 Task 1 carries a `<precondition>` making 13-01's consumed slot a stated input rather than an assumption. |
| Reviewer suggestion — "either temporarily bypass step 1 or scale `filter_len_mm` down (e.g. `/4.0f`)". | (suggestion) | **incorporated, narrowed** | The scale-down variant was taken and the bypass variant rejected: a full bypass would leave `g_psf_target_filt` unfiltered and re-seeded every tick, which defeats the single shared filter state the ordinary branch depends on. Divisor form keeps one filter state and two parameter sets, and keeps the `PSF_FILTER_MIN_MM` floor applied so the divisor cannot produce a zero-length EMA. |
