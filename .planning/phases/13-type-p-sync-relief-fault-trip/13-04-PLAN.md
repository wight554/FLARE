---
phase: 13-type-p-sync-relief-fault-trip
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md
  - scripts/verify_phase13_hw_log.py
  - scripts/test_verify_phase13_hw_log.py
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - REQ-type-p-sync-relief-no-relay-reintroduction
  - REQ-type-p-sync-relief-bounded-refill

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every rig step is written down before the rig is touched: the exact build to flash, the exact capture command, the exact print, and the four pass-bar comparisons against the 2026-09-12 baseline — so the 2 h print produces a verdict rather than an anecdote (D-30, D-32)"
    - "Pass-bar item 4 is expressed relative to the observed rail extremes, never as a literal position compare — the same discipline the firmware itself now follows (D-31.4)"
    - "Both new knobs are confirmed to survive a reboot via GET: before the print starts — a runtime SET that was never flashed would invalidate the whole capture (memory: rail-break 3000 needed a reflash)"
    - "No HW: item anywhere is checked off without the developer's explicit confirmation and real rig results (AGENTS.md rule 12); the checkpoint is the only place that confirmation can happen"
    - "The four pass-bar numbers are derived by a committed script, not by hand-rolled grep/awk at 2 a.m. after a 2 h print: scripts/verify_phase13_hw_log.py consumes the capture artifacts and prints one verdict line per pass-bar item, and it prints NEEDS-INPUT rather than PASS for any item whose input it was not given (REVIEW-09)"
    - "The parser is itself proven without a rig: scripts/test_verify_phase13_hw_log.py drives it over synthetic fixtures covering a passing capture, a failing capture and a missing-input capture, and runs in the ordinary regression gate"
  artifacts:
    - .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md (pre-flight, capture procedure, the four pass-bar comparisons with their baseline numbers, the exact parser invocation, and an empty results table)
    - scripts/verify_phase13_hw_log.py (stdlib-only pass-bar evaluator over the watch JSONL event log and an optional flare_sync_check CSV)
    - scripts/test_verify_phase13_hw_log.py (unittest.TestCase fixtures proving the evaluator's pass, fail and NEEDS-INPUT paths)
    - .planning/ROADMAP.md and .planning/STATE.md (Phase 13 outcome recorded after the checkpoint)
  key_links:
    - "13-HW-VALIDATION.md pre-flight GET: checks -> flashed firmware confirmed -> scripts/verify_hw_open_items.py watch --log <file> -> 2 h / ~10-swap print -> scripts/verify_phase13_hw_log.py over that log -> the same four event families baseline-capture.md measured -> pass/fail verdict per D-31"
    - "scripts/verify_hw_open_items.py run_watch writes JSONL records shaped {\"kind\":\"event\",\"time\":<epoch float>,\"type\":<EV type>,\"data\":<EV data>} (verify_hw_open_items.py:589) and {\"kind\":\"flowguard\",\"t\":<rel s>,\"level\":...} (:609) — that record shape is the parser's input contract and pass-bar items 1-3 are all event-count questions over it; item 4 is a POSITION question the event log cannot answer, so it needs a flare_sync_check.py --csv capture (BP column, scripts/flare_sync_check.py:78) or it stays NEEDS-INPUT"
    - "A failed pass-bar item does not fail the phase silently — it becomes a recorded gap that /gsd-plan-phase --gaps can pick up, with the captured log as its evidence"
  manual:
    - "HW: the entire 2 h A/B print is human-only; nothing in this plan can be auto-verified against a rig"
---

<objective>
Close Phase 13 with the one thing simulation cannot provide: real spring dynamics and real extruder grip.
Write the acceptance sheet first, run the same 2 h / ~10-swap print on the same rig with the same capture
tool that produced the 2026-09-12 baseline, and compare against the four pass-bar items agreed in
13-CONTEXT.md D-31. This plan is deliberately last and deliberately human-gated — D-32's "sim first,
filament last" is an ordering rule for the whole phase, not a preference.

Purpose: confirm the bounded relief actually removes the hunting cycle on hardware, and confirm none of
the three new automatic stop paths (distance trip, ms fallback, probe) false-fire on a print that
completes normally.
Output: a filled-in `13-HW-VALIDATION.md`, an updated ROADMAP/STATE, and either a closed phase or a
recorded set of gaps with rig evidence attached.
</objective>

<execution_context>
@~/.claude-personal/gsd-core/workflows/execute-plan.md
@~/.claude-personal/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-type-p-sync-relief-fault-trip/13-CONTEXT.md
@.planning/phases/13-type-p-sync-relief-fault-trip/baseline-capture.md
@.planning/phases/13-type-p-sync-relief-fault-trip/13-01-SUMMARY.md
@.planning/phases/13-type-p-sync-relief-fault-trip/13-02-SUMMARY.md
@.planning/phases/13-type-p-sync-relief-fault-trip/13-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the acceptance sheet and its log parser before the rig is touched</name>
  <files>.planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md, scripts/verify_phase13_hw_log.py, scripts/test_verify_phase13_hw_log.py</files>
  <read_first>
    scripts/verify_hw_open_items.py:570-620 (`run_watch` — the exact JSONL records the capture writes:
      `{"kind": "event", "time": <epoch float>, "type": <EV type>, "data": <EV data>}` at :589 and
      `{"kind": "flowguard", "t": <relative seconds>, "level": <level>}` at :609. This record shape is the
      parser's entire input contract; note the log is append-mode, so a resumed capture can contain more
      than one run and the parser must key relative time off the earliest record it sees, not assume t0);
    scripts/flare_sync_check.py:78-80 (`CSV_FIELDS` — `BP` is the buffer position column, alongside `BUF`,
      `SM` and `ST`) and :1089-1110 (the `--csv`, `--daemon`, `--duration` flags that produce it) — this is
      the only existing capture that answers pass-bar item 4, because the watch log carries events, not
      positions;
    scripts/test_verify_hw_open_items.py (the existing test module for the sibling script — copy its
      fixture style and its stdlib-only discipline);
    .planning/phases/13-type-p-sync-relief-fault-trip/baseline-capture.md (the whole file: the capture
      command, the print shape, the hunting cadence figures, the 13 `SYNC:TENSION_RISK_HIGH` timestamps,
      the observed `BS:` span, and the `UNLOAD_TIMEOUT` narrative that motivates the rail-relative rule);
    13-CONTEXT.md D-30/D-31/D-32 (the acceptance definition — transcribe the four pass-bar items verbatim
      rather than paraphrasing them);
    scripts/verify_hw_open_items.py:1-45 and :667-702 (the subcommands and the `watch` flags `--log` and
      `--interval` — quote the exact invocation, not an approximation);
    13-01/13-02/13-03 SUMMARYs (the final names of the two new knobs, the new `ST:` fields, and every new
      `EV:SYNC:*` event string — the sheet's grep recipes must match what actually shipped);
    .planning/phases/14-klipper-mmu-status-parity/14-VALIDATION.md (the front-matter and section shape used
      for a phase validation doc in this project — follow it).
  </read_first>
  <action>
    Create `13-HW-VALIDATION.md` with front matter matching the project's existing phase-validation doc
    shape, and these sections.

    **Pre-flight.** The exact build and flash steps for the phase firmware (dev-tuning superset per
    AGENTS.md, then flash), followed by a reboot and a `GET:` for each of the two new knobs confirming the
    values persisted rather than merely being set at runtime — a runtime-only `SET` invalidates the whole
    capture, which is exactly how the rail-break 3000 value was lost once before. Also record a `GET:` of
    the pre-existing tension dwell threshold so the mm-versus-ms split observed in the capture can be
    interpreted afterwards.

    **Capture procedure.** The exact `scripts/verify_hw_open_items.py watch` invocation with an explicit
    `--log` path, started before the print and left running for its duration; a second, concurrent
    `scripts/flare_sync_check.py --daemon --csv <path>` capture with an explicit duration covering the
    print, because the watch log records events only and pass-bar item 4 is a position question; the print
    itself defined as the same job used for the baseline (roughly 2 hours, roughly 10 tool swaps, same
    rig); and a note that neither capture file is committed to this repo, only the derived numbers.

    **Pass bar.** Transcribe D-31's four items with their baseline comparators made explicit and each one
    reduced to a countable check against the captured log:
    1. relief-pause rate at most one per 60 s during sustained sync — baseline was dozens roughly every
       3 s;
    2. fewer `SYNC:TENSION_RISK_HIGH` events than the baseline's 13 in 2 h;
    3. zero false stops on a print that completes normally — no fault-hold, no distance-trip event, no
       no-consumer probe verdict;
    4. no rail hits during print sync — the buffer position never comes within the break delta of either
       observed extreme outside deliberate BL/toolchange operations. State this comparison in terms of the
       extremes actually observed in the capture, and state explicitly that expressing it as a literal
       position threshold is forbidden and why (the `51bdca8` root cause).
    For each item give the concrete way to derive the number from the log — which is now a single
    invocation of `scripts/verify_phase13_hw_log.py`, quoted verbatim with both capture paths, not a
    hand-assembled grep/awk pipeline — and state that a partial result is a fail for that item, not a
    judgement call.

    **Write the parser (REVIEW-09).** Create `scripts/verify_phase13_hw_log.py` as a stdlib-only script
    (`json`, `csv`, `argparse`, `statistics` only — the repo's host scripts take no third-party
    dependencies) with `--events <jsonl>` for the `verify_hw_open_items.py watch` log and an optional
    `--csv <path>` for the `flare_sync_check.py` capture. It parses the JSONL record shape at
    verify_hw_open_items.py:589, derives relative time from the earliest record rather than assuming a t0,
    and prints one fixed-format line per pass-bar item — item number, observed value, baseline comparator,
    and one of `PASS` / `FAIL` / `NEEDS-INPUT` — followed by an overall verdict line, exiting non-zero if
    any item is `FAIL` or `NEEDS-INPUT`.

    Item 1: count `SYNC` relief-pause events and divide by the sustained-sync span the log covers,
    reported per 60 s against the baseline cadence. Item 2: count `SYNC:TENSION_RISK_HIGH` events against
    the baseline's 13. Item 3: count the fault-hold, `TENSION_STOP:MM`, `TENSION_STOP:MS` and
    `PROBE:NO_CONSUMER` events, each against a target of zero, taking the exact event strings from the
    13-01/13-02/13-03 SUMMARYs rather than from this plan's prose — if a name in the SUMMARY differs from
    what is written here, the SUMMARY wins and the discrepancy goes in this plan's own SUMMARY. Item 4:
    only computable with `--csv`; derive the observed minimum and maximum `BP` over the capture and count
    samples falling within `BL_BREAK_DELTA_NORM` of either observed extreme, expressed relative to those
    extremes and never against a literal position threshold (the `51bdca8` root cause — put that sentence
    in the script's own docstring so it survives a later edit). Without `--csv` the item prints
    `NEEDS-INPUT`, never `PASS`: a metric the script could not compute must never read as a pass.

    Create `scripts/test_verify_phase13_hw_log.py` as a `unittest.TestCase` subclass — discovery silently
    skips a module with no `TestCase` subclass, a trap this repo has hit before — with synthetic fixtures
    written to a temp directory covering three cases: a capture that passes all four items, a capture that
    fails at least items 2 and 3, and a capture with `--csv` omitted so item 4 reports `NEEDS-INPUT` and
    the overall exit status is non-zero. Assert on the per-item verdict tokens and the exit status, not on
    incidental formatting. These fixtures are the only proof the parser is correct, since the real capture
    exists once and cannot be replayed.

    **What sim already proved.** A short list of what must NOT be re-litigated on the rig because it is
    already deterministic (the bound invariant, trip ordering, arming, hold suppression, probe outcomes,
    shallow-rail twins), with a pointer to the scenarios, so rig time is spent only on what sim cannot
    reach (D-32).

    **Results.** An empty table with one row per pass-bar item: observed value, baseline value, verdict,
    and a notes column. Leave every verdict cell blank — Task 3 fills them in from real data only.

    Do not check off any `HW:` item anywhere in the repository in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md &amp;&amp; grep -c 'verify_hw_open_items.py watch' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md &amp;&amp; python3 -m unittest scripts.test_verify_phase13_hw_log -v 2>&amp;1 | tail -5 &amp;&amp; python3 scripts/validate_regression.py 2>&amp;1 | tail -5</automated>
    <fails_when>the file is absent; or the grep prints `0` (no exact capture invocation written down); or the unittest tail contains "FAILED", "ERROR" or "Ran 0 tests" (the latter means the module defined no `unittest.TestCase` subclass and discovery skipped it); or the regression gate output contains "FAILED" or "Traceback"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GET:' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md` is `>= 3` (both new knobs plus the dwell threshold in the pre-flight).
    - The four pass-bar rows exist and each names a countable log signal: `grep -c '^| [1-4]' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md` is `>= 4`, or the equivalent count for whatever list markup is used — state the count and the command in the SUMMARY.
    - `grep -c '0\.8\|0\.75\|-0\.99' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md` returns matches only inside the paragraph explaining why literal position thresholds are forbidden — confirm by `grep -n` and record the line numbers in the SUMMARY; pass-bar item 4 itself must contain no literal position threshold.
    - `grep -rl '\[x\].*HW:' .planning/phases/13-type-p-sync-relief-fault-trip || echo NONE` prints `NONE`.
    - The parser exists, is stdlib-only and is wired into the sheet: `grep -c 'verify_phase13_hw_log.py' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md` is `>= 1`, and `grep -cE '^(import|from) (requests|serial|numpy|yaml)' scripts/verify_phase13_hw_log.py` prints `0`.
    - The parser never passes an item it could not compute: `python3 scripts/verify_phase13_hw_log.py --events <fixture-with-no-csv>; echo "exit=$?"` prints a line containing `NEEDS-INPUT` for item 4 and `exit=` followed by a non-zero value (use the fixture the unit test builds; name the fixture path in the SUMMARY).
    - `python3 -m unittest scripts.test_verify_phase13_hw_log -v 2>&1 | tail -1` begins with `OK` and the run reports at least three tests; `grep -c 'unittest.TestCase' scripts/test_verify_phase13_hw_log.py` is `>= 1`.
    - `ruff check scripts/` exits 0.
  </acceptance_criteria>
  <done>A rig operator can execute the entire validation from this one file without re-reading CONTEXT.md, every pass-bar item reduces to a countable signal that a committed, unit-tested script derives from the captured log, no metric the script cannot compute can read as a pass, and no HW item has been checked.</done>
  <reversibility rating="reversible">A planning document plus a standalone host script and its test — no firmware, no runtime behaviour. Plain git revert.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Run the A/B print on the rig and report the four pass-bar results</name>
  <what-built>
    The complete Phase 13 type-P firmware: bounded rail-relative relief snap (13-01), the
    `SYNC_TENSION_STOP_MM` distance trip with arm-after-transition and hold suppression (13-02), and the
    feed probe with `PR:` telemetry and the `PROBE:` bench command (13-03) — all green in `flare_sim` at
    rail scales 1.0, 0.7 and 0.5, none of it yet run on filament. Plus
    `.planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md`, the acceptance sheet written
    in Task 1, which contains the exact flash steps, pre-flight `GET:` checks, capture invocations and the
    four pass-bar comparisons against the 2026-09-12 baseline — plus
    `scripts/verify_phase13_hw_log.py`, a unit-tested parser that derives all four pass-bar numbers from
    the captures so none of them has to be counted by hand.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md` end to end on the type-P
    rig:
    1. Build the dev-tuning superset, flash it, reboot, and run the pre-flight `GET:` checks. Confirm both
       new knobs report their expected values AFTER the reboot, not just after a `SET`.
    2. Start `scripts/verify_hw_open_items.py watch` with the log path from the sheet, and the concurrent
       `scripts/flare_sync_check.py --daemon --csv <path>` capture (item 4 needs buffer positions, which
       the watch log does not carry).
    3. Run the same print as the 2026-09-12 baseline: roughly 2 hours, roughly 10 tool swaps, same rig.
    4. Stop both captures and run `scripts/verify_phase13_hw_log.py --events <log> --csv <csv>` — it
       prints the four pass-bar lines and an overall verdict. Paste that output verbatim; do not
       re-derive the numbers by hand. If any item prints `NEEDS-INPUT`, say so rather than substituting a
       manual count — a missing input is a reportable gap in the capture, not a number to fill in.

    Report back, per pass-bar item, the observed number and the baseline number:
    1. Relief-pause events per 60 s of sustained sync (baseline: dozens, roughly every 3 s).
    2. Total `SYNC:TENSION_RISK_HIGH` count over the print (baseline: 13 in 2 h).
    3. Whether the print completed normally, and the counts of fault-hold, distance-trip and
       no-consumer-probe events (target: zero of each on a normal completion).
    4. The deepest and highest buffer positions observed during print sync, and whether the buffer came
       within the break delta of either of those extremes outside deliberate BL or toolchange operations.
    Also report anything the sheet did not anticipate — an unexpected event string, an audible near-slip, a
    swap that behaved differently from the baseline. Attach or paste the relevant log excerpts.
  </how-to-verify>
  <resume-signal>
    Reply with the `verify_phase13_hw_log.py` output pasted verbatim plus the four observed numbers and
    their verdicts. "all four pass" closes the phase; anything
    else — including a partial pass on any single item — is reported as-is with the log excerpts and routes
    to gap closure. Do not reply "approved" without the numbers: Task 3 is forbidden from filling a verdict
    cell from anything but this report.
  </resume-signal>
  <if_fails>
    Report which items failed with their observed numbers and the log excerpts. Do not check off any `HW:`
    item. Task 3 records the failure as a gap with the capture as evidence; the phase then routes to
    `/gsd-plan-phase --gaps` rather than being closed. A partial pass is a fail for that item — the pass bar
    is all four (D-31).
  </if_fails>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and close or open gaps</name>
  <files>.planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    The results reported at the Task 2 checkpoint (the only source for the verdict cells — do not infer,
      estimate or fill any cell from a sim result);
    .planning/ROADMAP.md (the Phase 13 block as updated by 13-01) and .planning/STATE.md (the Current
      Position and Accumulated Context sections, and the format prior phases used to record an outcome);
    13-03-SUMMARY.md (the three carried follow-ups — D-24 soft-wall blend, D-05 compression relief, D-13
      compression distance trip — which must land somewhere durable, not evaporate with the phase).
  </read_first>
  <action>
    Fill the Results table in `13-HW-VALIDATION.md` from the reported numbers only, one row per pass-bar
    item, including the notes column. Prefer the `verify_phase13_hw_log.py` output pasted at the
    checkpoint over any hand-derived figure, and record the exact invocation used alongside the table so
    the derivation is reproducible from the (uncommitted) capture. An item the script reported as
    `NEEDS-INPUT` is recorded as `NEEDS-INPUT` with the missing capture named — never promoted to a pass
    and never quietly filled from a manual count. If an item failed, write the observed number and what it
    implies rather than softening it.

    If all four passed: mark the phase's `HW:` acceptance satisfied in the validation doc, record the
    verdict in `.planning/ROADMAP.md` under Phase 13 and in `.planning/STATE.md`'s current position and
    accumulated context, and note the date and rig.

    If any failed: record the failure in the validation doc and in STATE.md as an open item with the
    capture as its evidence, leave every `HW:` item unchecked, and state in the SUMMARY that the phase
    routes to `/gsd-plan-phase --gaps` with a one-line description of each gap. Do not close the phase.

    Either way, carry the three deferred follow-ups from 13-03-SUMMARY.md into a durable location — the
    project backlog or STATE.md's accumulated context — so they survive the phase closing, and record any
    tuning observation the rig surfaced that would inform a future `SYNC_PSF_RELIEF_MULT` or
    `SYNC_TENSION_STOP_MM` default change.
  </action>
  <verify>
    <automated>python3 scripts/validate_regression.py 2>&amp;1 | tail -5 &amp;&amp; grep -c 'baseline' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md</automated>
    <fails_when>non-zero exit code; or the regression output contains "FAILED" or "Traceback"; or the grep prints `0` (the results were recorded without their baseline comparators)</fails_when>
  </verify>
  <acceptance_criteria>
    - Every Results row in `13-HW-VALIDATION.md` has a non-empty observed value, baseline value and verdict; no cell reads as a placeholder, and any `NEEDS-INPUT` verdict names the capture that was missing. Confirm by `grep -n` and record the check in the SUMMARY.
    - The derivation is reproducible: `grep -c 'verify_phase13_hw_log.py' .planning/phases/13-type-p-sync-relief-fault-trip/13-HW-VALIDATION.md` is `>= 2` (the procedure invocation plus the invocation actually used for the recorded results).
    - `grep -rl '\[x\].*HW:' .planning/phases/13-type-p-sync-relief-fault-trip || echo NONE` prints `NONE` unless the developer explicitly confirmed all four items passed at the Task 2 checkpoint, in which case the SUMMARY quotes that confirmation verbatim (AGENTS.md rule 12).
    - `grep -c 'D-24\|soft-wall blend' .planning/STATE.md` is `>= 1` — the carried follow-ups landed somewhere durable.
    - `sed -n '/^### Phase 13:/,/^### Phase 14:/p' .planning/ROADMAP.md | grep -c 'TBD'` still prints `0`.
    - `python3 scripts/validate_regression.py` exits 0.
  </acceptance_criteria>
  <done>The four pass-bar results are recorded against their baselines from real rig data, the phase is either closed with the developer's explicit confirmation or routed to gap closure with evidence attached, and the three deferred follow-ups are durable.</done>
  <precondition>The Task 2 checkpoint has been answered with real rig numbers. This task must not run against sim results or estimates — a verdict cell filled from anything other than the capture makes the whole acceptance sheet worthless.</precondition>
  <reversibility rating="reversible">Planning documents only. Plain git revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer report at the checkpoint → recorded verdict | The only channel by which physical evidence enters the planning record; nothing downstream can re-derive it. |
| Capture files → `verify_phase13_hw_log.py` → recorded numbers | An untrusted, uncommitted, 2 h machine-generated log is parsed into the verdicts that close or fail the phase; a parsing mistake or a missing input is indistinguishable from a clean result unless the script says so out loud. |
| Flashed firmware vs. runtime `SET` state on the rig | A knob set at runtime but never persisted makes the capture measure a configuration that will not survive the next reboot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-16 | Repudiation | A pass-bar verdict recorded without rig evidence | high | mitigate | Task 3 carries a `<precondition>` forbidding any verdict cell filled from anything but the capture; the checkpoint is `blocking-human`; an acceptance criterion requires the developer's confirmation to be quoted verbatim before any `HW:` item can be checked (AGENTS.md rule 12). |
| T-13-17 | Tampering | Capture taken against unpersisted runtime knob values | medium | mitigate | Pre-flight `GET:` checks after a reboot are step 1 of the checkpoint and an acceptance criterion of Task 1, precisely because this failure has happened before on this rig. |
| T-13-26 | Repudiation | `verify_phase13_hw_log.py` reporting a verdict for a metric it could not compute | medium | mitigate | REVIEW-09. Automating the pass bar removes hand-counting errors but introduces a new way to launder a non-result into a PASS — an item whose input capture was missing, or whose event name drifted from what the SUMMARYs actually shipped, could silently read as zero occurrences and therefore as a pass. Mitigated by three rules baked into Task 1: the script prints `NEEDS-INPUT` (never `PASS`) for any item it lacks input for and exits non-zero; it sources the event strings from the 13-01/13-02/13-03 SUMMARYs rather than this plan's prose, with any discrepancy recorded; and `scripts/test_verify_phase13_hw_log.py` fixtures assert the pass, fail and NEEDS-INPUT paths explicitly. Task 3 records `NEEDS-INPUT` verbatim rather than promoting it. |
| T-13-18 | Denial of Service (physical) | A new stop path false-firing mid-print during the validation run | medium | accept | This is the measurement, not a defect in it: pass-bar item 3 exists to detect exactly this, and a false fire stops a print rather than damaging hardware. The `if_fails` branch routes it to gap closure with the capture attached. |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed; this plan writes planning documents and runs existing repo scripts. |
</threat_model>

<verification>
- `python3 scripts/validate_regression.py` green (this plan changes no code, but the gate must stay green
  at phase close per AGENTS.md rule 1).
- The Task 2 checkpoint answered with real numbers for all four pass-bar items.
- No `HW:` item checked anywhere without the developer's explicit confirmation quoted in the SUMMARY.
</verification>

<success_criteria>
- Phase 13 Success Criterion 5's hardware half is resolved either way: `HW:` items are checked only on the
  developer's explicit confirmation with real rig results, or they remain unchecked with the failures
  recorded as gaps.
- The four D-31 pass-bar items each have an observed number, a baseline number and a verdict, with pass-bar
  item 4 expressed relative to the observed extremes.
- D-32's ordering held: everything deterministic was proven in `flare_sim` across plans 13-01 to 13-03
  before any filament was spent.
- The three carried follow-ups (D-24, D-05, D-13) survive the phase in a durable location.
</success_criteria>

<output>
Create `.planning/phases/13-type-p-sync-relief-fault-trip/13-04-SUMMARY.md` when done.
</output>

## Review Dispositions

Cross-AI review source: `13-REVIEWS.md` (antigravity lane, 2026-09-12). Every finding that names plan
13-04 is listed here with its disposition. Findings addressed to 13-01/13-02/13-03 are dispositioned in
those plans.

REVIEW-NN labels are this phase's stable handles for the findings and are the strings that appear in
source comments, threat rows and scenario comments. The map across all four plans is fixed:
REVIEW-01 13-01 EMA lag - REVIEW-02 13-03 point-in-time probe sample - REVIEW-03 13-03 threshold
inversion - REVIEW-04 13-02 hold falling edge - REVIEW-05 TLV clamp (13-01 and 13-02 halves) -
REVIEW-06 13-02 cannot_refill dormancy - REVIEW-07 13-01 extreme staleness - REVIEW-08 13-03 PROBE:
guard conditions - REVIEW-09 13-04 log-parsing automation.

| Finding | Severity | Disposition | Rationale |
|---------|----------|-------------|-----------|
| **REVIEW-09** — Log processing volume: a 2 h continuous `verify_hw_open_items.py watch` capture produces substantial data, and manual grep/awk derivation of the four pass-bar metrics invites human calculation errors. Suggested fix: a companion script that consumes the capture and evaluates the four metrics against the baseline. | LOW | **incorporated** | Task 1 now produces `scripts/verify_phase13_hw_log.py` alongside the acceptance sheet rather than as a separate task, because the sheet and the parser that fills it are one deliverable and splitting them would leave a sheet whose recipes nothing executes. The script is stdlib-only, parses the JSONL record shape at verify_hw_open_items.py:589, prints one `PASS`/`FAIL`/`NEEDS-INPUT` line per pass-bar item plus an overall verdict, and exits non-zero on anything but four passes. `scripts/test_verify_phase13_hw_log.py` proves it over synthetic pass/fail/missing-input fixtures with no rig involved. Frontmatter `files_modified`, `must_haves.truths`, `artifacts`, `key_links`, Task 1 `<verify>`/`<acceptance_criteria>`, the checkpoint's `<how-to-verify>`/`<resume-signal>`, Task 3's action and threat `T-13-26` all follow. |
| REVIEW-09, item-4 sub-case — pass-bar item 4 is a buffer-POSITION question. | (derived) | **incorporated, scope named explicitly** | The watch log carries events only (verify_hw_open_items.py:589), so item 4 is not derivable from it at all — a gap the original sheet did not name. Rather than invent capture infrastructure, the plan points at the existing `scripts/flare_sync_check.py --daemon --csv` capture (its `BP` column, flare_sync_check.py:78) as a concurrent second capture, and requires the parser to print `NEEDS-INPUT` for item 4 when `--csv` is absent. Item 4 stays expressed relative to the observed extremes, never against a literal position threshold, and that constraint is required to appear in the script's own docstring so it survives a later edit (`51bdca8` root cause). |
| Reviewer risk assessment — "**LOW**. Fully human-gated, clearly scoped, and methodologically sound." | (summary) | **acknowledged, no change** | The human gate, the `<precondition>` forbidding sim-derived verdict cells, and AGENTS.md rule 12 enforcement are unchanged by this pass. Automating the derivation does not weaken the gate: the script produces numbers, the developer still supplies them at a `blocking-human` checkpoint, and Task 3 still cannot fill a cell from anything else. |
