# Phase 3 Plan: Klipper Event-Driven Mirror & Slicer Purge

**Goal**: Eliminate 4Hz serial polling in `flare_daemon.py` and add dynamic OrcaSlicer purge volume tracking.

## Tasks
- [x] In `scripts/flare_daemon.py`:
  - Convert periodic 4Hz `SET_MMU` delta push into event-driven push triggered by firmware events (`EV:`)
  - Latch cosmetic piston position monotonically to reduce Fluidd UI update thrash
- [x] In `klipper/mmu.py` & `klipper/flare_mmu.cfg`:
  - Implement `_FLARE_SET_PURGE PURGE=<float>` macro to receive OrcaSlicer per-transition purge volume
  - Override default static `purge_len` with dynamic transition volume during toolchange
- [x] Validate Python compilation and macro dry-runs
