# Plan 12-01: Post-Phase 2–10 Regression Fixes

**Goal**: Land every requirement in `12-SPEC.md` as small verified commits; sim/unit coverage for each behavioural fix; docs in the same commit as the code.

## Tasks (one commit each unless noted)
- [x] **T1 Toolchange park** (`12-SPEC` §1) — `toolchange.c`, `gen_config.py`, `config.ini.example`, `MANUAL.md`, `BEHAVIOR.md`. Park only after TS-edge completion; COMPRESSION stop during park = success (clear `FAULT_BUF`, emit `TC:TS_PARKED`); type-P skips park; default `tc_ts_park_mm` → 0 (Klipper `_FLARE_CHANGE_LANE` already does `G1 E{load_park_dist}`); retry exhaustion → `TC:ERROR:TS_NOT_HIT`; no retry on RUNOUT. Sim scenario in `test_sync_sim.py` or `sim_scenario.c`.
- [x] **T2 Cutter abort settle** (§2) — `cutter.c`: `CUT_ABORT_SETTLE` state holds `SERVO_BLOCK_US` for `servo_settle_ms` before `servo_idle()`; `cutter_busy()` true during settle.
- [x] **T3 Bare BL passive** (§3) — `sync.c`: no follow arming without explicit args; `flare_mmu.cfg` retract guard passes LENGTH; sim assert bare-BL passivity (D and P).
- [x] **T4 GET:BUF_POS_RAW read-only + type gate** (§4) — `protocol.c`.
- [x] **T5 TMC heartbeat** (§5) — `motion.c`, `protocol.c`: sync-state gate, non-blocking backoff state machine, fault latch after 3 failures, no `stop_all` while BL held; `test_tmc_recovery.c` updated.
- [x] **T6 STOP/PA keep TH latch** (§6) — `protocol.c`, `MANUAL.md`.
- [x] **T7 BS no-op no force-zero** (§7) — `sync.c`.
- [x] **T8 --dump rebuilds** (§8) — `flare_cmd.py`, `test_settings_parity.py`.
- [x] **T9 Daemon host decision + trust-proxy auth** (§9) — `flare_daemon.py`, `flare_daemon.service`, docs; record decision in this file.
- [x] **T10 Mirror retry + lane task keys** (§10) — `flare_daemon.py`, tests.
- [x] **T11 Test collection + analyzer safe mode** (§11) — 4 test files → TestCase; `validate_regression.py`; `flare_analyze.py`.
- [x] **T12 Docs & style parity** (§12) — `MANUAL.md`, `motion.c`, `settings_store.c`, `protocol.c`, `toolchange.c`.
- [x] **T13 Roadmap/state close-out**.

## Success criteria
`12-SPEC.md` success criteria 1–11; `python3 scripts/validate_regression.py` green after every commit; `HW:` items left unchecked.

## Decision D12.1 — daemon bind host (supersedes audit-hardening-fixes 4.1 and 38de2a7)
Default `--host 0.0.0.0` again. 4.1 reverted the loopback default on 2026-06-19 because it silently
removed the LAN dashboard on reinstall; 38de2a7 re-introduced loopback for security before Phase 9
existed. With Phase 9 in place a LAN bind is safe: loopback peers stay exempt, remote mutations need
the auto-generated Bearer token, reads stay public. `--trust-proxy` now forces `AUTH_REQUIRED`
regardless of bind host (behind nginx the peer is always loopback). Operators who want loopback-only
add `--host 127.0.0.1` to the unit; `install_daemon.py` preserves it.

## Validation (2026-09-11)
Commits: 842dbb0 (T1) 8dd159b (T2) c305fa2 (T3) 84c6132 (T4/T6/T7) 12b3d5d (T5) ba325f9 (T8) b783148 (T9/T10) 339cd0c (T11) 8aaba7e (T12).
- `python3 scripts/validate_regression.py` → Static Regression Gate Passed after every commit.
- Python: 261 tests via `unittest discover` (was 133 — 128 were silently uncollected).
- Host units: `test_toolchange` (5), `test_cutter` (2), `test_tmc_recovery` (5, incl. sync-state lockout + zero-sleep), `test_forensics` (9), `test_persistence` — all run by `scripts/test_host_units.py`.
- Sim: 47 scenarios incl. new `sem_bl_bare_passive` (D and P); trace gained `motor_sps`.
- §10.2 `lane*_task` keys: already present in `keys_to_check` — no change needed.
- HW (unchecked): type-D toolchange w/ `TC_TS_PARK_MM>0` → no `FAULT:MOVE_COMPRESSION`; cutter abort mid-stroke returns to block; bare `BL:T` under retract stays passive; TMC unplug → single `TMC:FAULT`, `TMC:RESTORED` on replug.

## Decision D12.2 — commit attribution (task-workflow spec updated)
Claude `Co-Authored-By` is required only when Claude produced or substantially assisted the commit;
a non-Claude tool alone carries just its `Generated-By:` line. Matches AGENTS.md "Preserve attribution".

## Follow-ups filed (grill 2026-09-11)
- Rig checklist for Phase 11 + 12 items → `TEST_CASES.md` "Pending Phase 11–12 Rig Validation" (incl. `SET:TC_TS_PARK_MM:0` + `SV:` if flash holds 25, and the open D2 half-cap question).
- `.planning/backlog/settings-store-table-and-tier-gaps` (TLV table refactor, cutter timeout tiers).
- `.planning/intel/decisions.md` now points at `.planning/decisions/archive`.
