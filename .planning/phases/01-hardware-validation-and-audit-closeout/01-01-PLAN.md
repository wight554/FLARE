# Phase 1 Plan: Hardware Validation Closeout

**Goal**: Execute pending real-hardware tests on physical test rig for code-complete firmware fixes.
**Status**: Ready for physical bench session

## Tasks

### 1. Audit Hardening Rig Validation
- [x] Test `CAL` command issued during active motion -> assert returns `ER:PERSIST_BUSY` without corrupting flash (2026-09-12, `verify_hw_open_items.py fire 1-cal-busy` on rig: `ER:PERSIST_BUSY` observed, PASS)
- [x] Test cutter command `CP` issued mid-cut -> assert returns `ER:BUSY` and initial cut completes cleanly (2026-09-12, `verify_hw_open_items.py fire 2-cp-busy` on rig: `ER:BUSY:CUTTER` observed, PASS)
- [x] Test `T:` / `TC:` issued during active toolchange -> assert returns `ER:BUSY`, toolchange unaffected (2026-09-12, `verify_hw_open_items.py fire 3-tc-busy` on rig: `ER:BUSY:TC` observed, PASS)
- [x] Test buffer-lock timeout -> assert emits single-prefix `EV:BL:TIMEOUT` visible in daemon history (2026-09-12, `verify_hw_open_items.py watch` on rig: single clean `EV:BL:TIMEOUT` observed twice, ~39s apart, neither a storm, PASS)

### 2. Audit Reliability Rig Validation (Type-P Rig)
- [ ] Runout RELOAD end-to-end + manual `RL:` on Type-P rig -> assert contact on compression, success on grab
- [x] Verify no instant `RELOAD:LOADED` false positive without filament motion (2026-09-14, `flare_cmd.py RL:` on rig with both lanes loaded and extruder idle: `OK` + single `EV:RELOAD:LOADED:1`, no lane task, buffer position unchanged, no `FOLLOW_JAM`, PASS)
- [ ] Test paused/no-consumer `RL:` completes on staged compression without `FOLLOW_JAM`
- [ ] Re-issue `RL:` immediately after completed reload with consumer active -> assert `RELOAD:LOADED` with no motion and no `FOLLOW_JAM`

### 3. Flash Wear & Runout Race Checks
- [ ] Perform repeated settings saves on real RP2040 hardware -> assert wear counter increments and persists across reboots
- [ ] Verify genuine complete runout on Type-P rig escalates cleanly to RELOAD without race stalling

## Success Criteria
All 6 physical hardware verification checks pass with real telemetry traces recorded.
